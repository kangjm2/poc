target/
node_modules/
dist/
.backend.pid
.backend.log
.frontend.pid
.frontend.log
*.class
.DS_Store

# Compose overrides: .env holds real credentials and ports for a given host.
# .env.example is the tracked template.
.env
docker-compose.override.yml

# TypeScript incremental build cache — machine-local, changes on every build
*.tsbuildinfo
