# UI 격차 분석 — 실제 스크린샷 대비

**질문:** 매뉴얼 텍스트만 읽고 UI를 "창작"한 것은 아닌가? 기존 Keysight 툴을 쓰던
통신사 직원이 이 도구를 열었을 때 이질감을 느끼지 않는가?

**방법:** 매뉴얼 기재 항목이 아니라 **화면 이미지**를 근거로 삼았습니다.
`docs/assets/screenshots/`의 실제 제품 스크린샷을 열어 화면 요소를 하나씩 세고,
`docs/poc-screenshots/`의 우리 화면과 대조했습니다. 아래 모든 "레퍼런스" 항목은
이미지에서 직접 판독한 것이며, 매뉴얼 문장에서 옮긴 것이 아닙니다.

기준 이미지 4종:

| 파일 | 무엇 |
|---|---|
| `nemo-analyze_workbook_line-and-bar.png` | Analyze 워크북 (사후 분석 — 우리 Analysis 화면의 직접 대응물) |
| `nemo-outdoor_5g-nr_main-window_2560x1440.png` | Outdoor 실시간 측정 (연결·수집 중 화면) |
| `nemo-analyze_troubleshooting.jpeg` | 자동 문제 조사 → 드릴다운 → 원인 규명 |
| `s8709a-vdt_fig2-equipment-chain.png` | VDT 툴셋 구성 (Nemo → UXM → PROPSIM → DUT) |

---

## 1. 결론부터

**창작이 아닙니다.** 우리 Analysis 화면의 골격은 Analyze 워크북과 같은 배치입니다 —
좌측 Parameters 도크, 중앙 지도+그래프 스택, 우측 Color Legends / Numerical Data 도크,
하단 워크북 탭, 하단 상태 바. Outdoor의 START/END/CURRENT 진행 바와 임계 강조(노랑/빨강)도
같은 형태입니다.

**그러나 빠진 것이 분명히 있었습니다.** 최초 판정(2026-08-30) 시점에 기존 사용자가
즉시 알아챌 부류로 세 가지를 꼽았고, 셋 다 이후 증분에서 닫았습니다.

1. ~~**접속·RACH 계열 화면이 통째로 없음**~~ — `5G NR RACH metrics` 도크와 서빙 셀 식별
   테이블을 구현했습니다(`Lab` → 런 상세). §3 참조.
2. ~~**차트 종류가 라인 하나뿐**~~ — 셀별 **바 차트**(`Cells` 탭)와 원인별 **파이 차트**
   (`Problem Survey` 탭)를 추가했습니다. §2·§4 참조.
3. ~~**드릴다운 연쇄가 없음**~~ — "원인 집계 → 개별 사례 → 그 순간" 3단을 구현했습니다.
   §4 참조.

**지금 남은 것은 성격이 다릅니다.** 아래 표에서 ⛔로 남은 항목은 대부분 UI를 안 만든 것이
아니라 **받쳐 줄 데이터가 없는 것**입니다(이웃 셀 measurement, 주행 영상). 이 구분은
§6에 정리했습니다.

> **표 읽는 법**: §2–§4의 판정 열은 **2026-08-31 재검토 기준**입니다. 최초 판정에서 바뀐
> 행에는 바뀐 사유를 함께 적었습니다.
>
> **2026-09-01 — 위 "방법"은 §8로 갱신됐습니다.** 이 문서는 화면 이미지만 근거로 삼아
> 썼고, 그때는 그것이 최선이었습니다(가진 매뉴얼이 절차 0건의 마케팅 자료였으므로).
> 이제 **505페이지 사용자 가이드**가 들어왔고, 이미지만으로는 알 수 없던 것들이
> 드러났습니다 — **뒤집힌 판단 넷과 하지 않기로 한 것 다섯**이 §8에 있습니다.
> §2–§4의 개별 행 중 매뉴얼로 정정된 것은 해당 행에 표시했습니다.

---

## 2. Analysis 화면 — Analyze 워크북 대비

| 레퍼런스 화면 요소 | 우리 상태 | 판정 |
|---|---|---|
| 좌측 Workspace 도크: Folders / Measurements / **Parameters** 3단 | Parameters 트리만, 세션은 상단 드롭다운 | ◐ 배치는 다르나 기능은 있음 |
| Parameters 트리 위의 **검색창** | **없었음 → 이번에 추가** | ✅ 해소 |
| Measurements 목록 + 검색창 | 드롭다운 | ◐ 세션이 수십 개가 되면 부족 |
| 기술별 파라미터 카테고리 트리 (AMPS/CDMA/GSM/LTE/…) | 기능별 카테고리 (Radio Quality/Throughput/…) | ✅ 동등 |
| 워크북 안에 **여러 개의 그래프 페인 스택** | 내장 탭은 고정, **구성 워크북은 페인을 쌓음** | ✅ **해소.** `+` 로 탭을 만들고 `+ Chart pane`/`+ Map pane` 으로 스택 |
| 페인별 **Tools / Layers** 도크 | 구성 워크북 페인마다 **Layers** 도크, 내장 지도 셋에는 지도 **Layers** 목록 | ✅ *2026-09-03 정정 — 아래 ◐ 판정은 낡았습니다.* 체크는 숨기기이지 삭제가 아니고(레퍼런스와 동일), **MAP 페인도 자기 레이어를 읽습니다** — 지도 페인이 차트 페인과 같은 이유로 **자기 트랙을 가져오고**(`ComposedWorkbook.tsx`의 `mapWants`·`tracks` 효과), 체크된 레이어의 트랙이 `RouteMap`으로 넘어갑니다. 지도는 색 스케일 하나를 칠하므로 지도 페인의 체크는 **배타적**입니다. 내장 지도 셋(Overview·Mobility·Coverage Issues)에도 레이어 목록이 있습니다 — `view/maplayers.ts`의 `describeLayers`가 **지도에 넘긴 그 객체**(`App.tsx`의 `mapContents`)에서 목록을 유도하고 우측 도크가 그것을 그립니다. **남은 것은 Tools(줌·마커)뿐입니다** — 이 앱의 어떤 차트에도 사용자가 조작하는 줌이 없습니다. 원래 판정 — ◐ **차트 페인만 해소**: *"MAP 페인에서는 도크가 무력하고(`visible[0]`이 캡션에만 쓰이고 오버레이 prop이 넘어가지 않음) 지도 자체에도 레이어 목록이 없습니다."* (줄번호 인용은 지웠습니다 — §6(b)의 P3-5 줄이 같은 이유로 지운 그것입니다) |
| 페인별 **Numerical Data** (커서 시점 값 테이블) | 있음 (우측) | ✅ |
| **Color Legends** (구간·건수·비율) | 있음 | ✅ |
| **바 차트** (`RSCP monitored set` — 셀별 막대) | `Monitored Set` 탭 — **커서 시점 모니터드 셋 막대**(PCI/Ch 2단 축) + `Cells` 탭의 서빙 PCI별 막대 | ✅ **축까지 해소.** 레퍼런스와 같이 *커서 순간*을 그림 — 집계가 아님. 옆 도크 표와 같은 응답에서 그려 두 패널이 어긋날 수 없음 |
| monitored set 테이블 (Ch/SC/RSCP, Ch/SC/Ec/N0) | 우측 도크 `Monitored Set` — Ch/PCI/RSRP/RSRQ/Δ | ✅ **해소** (V7 `sample_neighbour`). 레퍼런스는 RSCP·Ec/N0 두 표로 나눠 놓았지만 같은 행을 두 곳에서 읽게 되므로 한 표에 합쳤습니다. 서빙 셀은 맨 위 고정 — 0.1 dB 반올림 동률에서 순서가 뒤집히면 "단말이 쓰지 않는 셀"이 1위로 보입니다 |
| 시간 축 가로 스크롤(팬) | 범위 필터로 대체 | ◐ |
| 하단 **Activity** 탭 | `Import` 화면의 진행 표시 + `Import history` 표 | ✅ *2026-09-03 분리·정정.* 도는 잡의 **읽은 행 수**와 **Stop** 버튼이 진행 표시 옆에 있고(`ImportView.tsx` — 임포트가 도는 동안에만 0.7초 간격으로 잡을 다시 읽습니다), 아래에 최근 잡 이력 표가 있습니다. 레퍼런스의 **`Cancel All`은 없습니다.** 다만 우리 임포트는 파일을 여럿 골라도 **한 번에 하나씩 순서대로** 돌므로(`ImportView`의 submit 루프 — 같은 테이블에 대한 트랜잭션입니다) `RUNNING`은 많아야 하나이고, 없는 것은 버튼이 아니라 **동시에 여러 개가 돌 때만 뜻이 생기는 조작**입니다 |
| 하단 **Log window** 탭 (p213) | 없음 | ⛔ 프로그램·오류 메시지와 **처리 중인 SQL 문**. 우리는 사용자가 SQL을 볼 일이 없는 구조입니다 |
| 상태 바: Ready / **No global filters** / No scheduled events / Query memory | START/END/CURRENT/세션명 + 전역 필터 바 | ✅ *2026-09-03 정정.* 원문과 **같은 문구**로 "No global filters"를 적고(`GlobalFilterBar.tsx`), 걸려 있으면 서버가 풀어 쓴 조건과 `Reach: n of m`을 함께 적습니다. P3-2에서 나왔는데 이 줄만 ◐로 남아 있었습니다. *2026-09-03 보강: 이 바는 Analysis뿐 아니라 **Compare에도** 뜹니다(`App.tsx` — `mode === 'analyze' || mode === 'compare'`). 대칭을 위해서가 아니라 `/cohorts`가 **필터를 지키기 때문**입니다(`GlobalFilter.coverage()`) — 조건이 걸린 채 화면을 바꾸면 코호트 표가 부분집합 위에서 계산되는데, 바가 없으면 그 사실을 말하는 것도 푸는 것도 화면에 없습니다.* |
| 리본 (File/View/Tools/Utilities/Commander/Layout/Graph/Help) | 단일 툴바 | ◐ 의도적 단순화 |

## 3. 접속·수집 화면 — Outdoor 대비 (**가장 큰 공백**)

Outdoor 메인 윈도우에서 직접 판독한 요소입니다.

| 레퍼런스 화면 요소 | 판독한 실제 필드 | 우리 상태 |
|---|---|---|
| **`5G NR RACH metrics` 도크** | RACH type(Contention based), RACH reason(Channel request), RACH result(Succeeded), access delay(31 ms), config(98), contention resolution, logical root sequence(106), maximum preamble, **pathloss(95.0 dB)**, preamble count(1), **preamble format(Format A2)**, preamble index(3), preamble initial power(-3.0 dBm), preamble response, preamble step, PUSCH power(0.0 dBm), **RA-RNTI(267)**, response window(10 slot), SSB ID(0), **timing advance(2)** | ✅ 해소 — 19개 필드 중 **17개** 구현(`Lab` → 런 상세, `5G NR RACH metrics` 도크). `maximum preamble`·`preamble response` 2개만 미구현. 단 레퍼런스는 *실시간 수집* 중 상시 표시, 우리는 *랩 런* 화면 |
| **서빙 셀 식별 테이블** | Cell type(SCG PSCell), SSB band(NR n78), SSB NR-ARFCN(633984), PCI(8), SSB GSCN(7853) | ✅ 해소 — 5개 필드 전부 + `TA offset`(`Lab` → 런 상세, `Serving cell`) |
| `5G NR key parameters` 도크 + 임계 강조 | BLER 9.96% 노랑, TX power 19.4 dBm 빨강 | ✅ 동등 |
| 상단 **트랜스포트 컨트롤** (record/pause/stop) | 없음 (사후 분석 전용) | ⛔ |
| 하단 진행 바 + START/END/CURRENT | 있음 | ✅ |
| 워크북 탭 + **`+` (탭 추가)** | 내장 **14개** + **사용자 워크북** | ✅ 해소. 서버 저장이라 새로고침·다른 브라우저·동료에게도 남습니다. *(2026-09-03: 13개라고 적혀 있었습니다. `App.tsx`의 `WORKBOOKS`는 14행입니다 — Overview·Radio Quality·Throughput·Fronthaul·Cells·Monitored Set·Mobility·L3 Signalling·Problem Survey·Degradation·Coverage Issues·Statistics·Field-to-Lab·Compare on the Ground)* |
| 탭 구성 자체 (`5G RACH and Signalling`, `5G NR Beams`, `5G Physical Layer`) | RACH·Signalling은 있음. Beams·Physical Layer 탭 없음 | ◐ Beams는 SSB 빔 measurement가 없어 막힘(데이터), Physical Layer는 미구현(UI) |
| 면적 차트 2계열 (scheduled vs actual throughput) | 단일 라인 | ◐ |
| 상태 바 `Measurement: OnePlus 7 5G Oulu center 19Nov08 091517.1` | 세션명 표시 | ✅ |

## 4. 문제 조사 — Analyze Troubleshooting 대비

레퍼런스의 조작 연쇄를 그대로 옮기면:

```
원인별 파이 차트 (Call drop analysis)
   9.68% Unknown reason · 16.13% Missing neighbour · 3.23% Bad DL coverage, bad UL quality
   9.68% Bad DL coverage · 32.26% CC cause: Normal, unspecified · 29.03% Missing handover
        │  우클릭 → [Drill Down ▶]
        ▼
개별 사례 그리드
   Measurement · Event ID(CAD) · Time · Lat · Long · System · Cell ID · Carrier
   · Scr. code · Miss. nbor. scr. · Ec/No · RSCP · BLER · DL pwr up %
        │  행 선택
        ▼
그 순간의 워크북
   동기화된 라인 그래프(Ec/No·BLER·TX power) + Layers 체크박스 + Numerical Data
   + 지도(경로 + 사건 마커) + 시그널링 목록 + **디코드된 RRC 메시지 본문**
```

| 요소 | 우리 상태 |
|---|---|
| 원인 **분류·집계** | ✅ 해소 — 7개 원인(무선 링크 실패 / 약전계 / 간섭 / 셀 오버슈트 / 높은 BLER / 프론트홀 타이밍 / 처리율 열화). `Missing neighbour`는 아직 없으나 **"이웃 셀 데이터가 없어 산출 불가"라는 기존 설명은 틀렸습니다** — §6(a) 참조 |
| 파이 차트 | ✅ 해소 (`Problem Survey` 탭) |
| Drill Down 컨텍스트 메뉴 | ✅ 해소 — 조작은 동등하되 **우클릭 메뉴가 아니라 조각·행 클릭**. 기존 사용자에게 이질적일 수 있는 지점 |
| 개별 사례 그리드 | ✅ 해소 — 원인별 사례 그리드(시각·구간·심각도·값). Degradation/Coverage Issues 탭은 그대로 유지 |
| 사례 → 시각 이동 | ✅ 커서 이동 |
| 시그널링 목록 + 커서 추종 | ✅ |
| **메시지 본문 디코드** (3GPP 절 인용까지) | ◐ 2행 펼침 |
| 드릴다운 **다중 탭** (좌측, 조각 색) | ✅ **완료 (2026-09-03).** 원인 탭이 **열린 순서대로 쌓이고** 각 탭의 색이 그 조각 색입니다. 매뉴얼 p88이 스타일이 아니라 기능이라고 말한 그대로: *"Each drill-down … will open a **new tab** … with the **colors of the corresponding sectors**"* |

## 5. VDT 구성 — S8709A 대비

`s8709a-vdt_fig2-equipment-chain.png`가 보여주는 실제 체인:

```
Nemo Tools ──Field-To-Lab Conversion──▶ UXM 5G Wireless Test Platform ──▶ PROPSIM 5G Channel Emulator ──▶ Device Under Test
(필드 캡처)                              (5G NR 네트워크 에뮬레이트)      (필드 측정 채널 조건 재현)        (자동 성능 검증, OTA 챔버)
                                                    ▲
                                          Test System PC — test case execution, analysis and reporting
```

**우리 상태**: *2026-09-03 정정 — §7.1과 §7.3이 이 항목을 **완료**로 적은 뒤에도 아래 문단이
현재형으로 남아 있었습니다.* **그 사이는 채워졌습니다.** `BringUpPanel`이 장비 체인을 신호가
지나는 순서로 그리고(`Chain` — 역할·모델·펌웨어·시리얼·주소, 도는 단계의 장비가 강조됨), 셀 상태
스트립(`CellStrip`)과 단계별 표(`PHASE_LABEL` — `Field-to-lab` · `Instruments` · `RF path` ·
`Attach` · `Traffic`), RACH 도크, 서빙 셀 표, Duration·Progress·Pass rate 게이지가 한 화면에
있습니다. 상태도 건너뛰지 않습니다 — `test_run.status`는 `QUEUED|RUNNING|COMPLETED|FAILED|ABORTED`
이고(V2), `LabService.start()`가 단계들을 벽시계 위에 늘어놓으면 `RunAdvancer`가 3초마다 각
단계를 `PENDING → RUNNING → OK`로 옮기고 진행률을 **끝난 단계의 비율**로 씁니다. 아래는 그것이
없던 시절의 서술이고, 남은 차이(Project/Campaign 드롭다운 · 테스트 케이스별 리포트 링크 · 주행
영상 동기화 — §7 참조)를 읽는 데만 씁니다. 원래 서술:

> 우리 Lab Campaigns 화면은 **설정과 판정은 있으나 그 사이가 비어 있습니다.**
> 상태가 `QUEUED`에서 `COMPLETED`로 건너뛰고, 장비 체인·링크 상태·UE 접속 절차가 없습니다.
> 현실감의 공백이 여기 있습니다.

---

## 6. 남은 격차 — 세 부류로 나누어

최초 우선순위표(P0 접속/브링업·바 차트, P1 원인 분류·파라미터 검색창)는 **전부 닫혔습니다.**
갱신된 표는 §7.3에 있습니다. 남은 것을 성격별로 나누면 이렇습니다. **막힌 이유가 다르면
해결 비용도 다르므로** 한 표에 섞어 두지 않았습니다.

### (a) 데이터 모델이 막고 있는 것 — UI 작업이 아님

> **2026-09-01 갱신**: 이 목록의 네 항목이 **한 번에 닫혔습니다.** 넷 다 원인이 하나였기
> 때문입니다 — `sample`이 서빙 셀만 담았다는 것. V7이 `sample_neighbour`를 추가하면서
> monitored set 표·이웃 셀 바 차트 축·이웃 셀 연결선이 함께 열렸고, 파일럿 오염 검출이
> 새로 가능해졌습니다. **이것이 이 분류를 만든 이유입니다**: 막힌 원인이 같으면 해결도
> 같이 됩니다.

| 항목 | 필요한 것 | 상태 |
|---|---|---|
| ~~monitored set 테이블 (§2)~~ | `sample_neighbour` | ✅ V7 |
| ~~이웃 셀 바 차트 축 (§2)~~ | 위와 동일 | ✅ V7 |
| ~~서빙 셀 → *이웃* 셀 연결선 (§1.2)~~ | 위와 동일 | ✅ V7 |
| ~~`Missing neighbour` 원인 분류 (§4)~~ | ~~**설정된** 이웃 목록~~ | ⚠ **판정 철회 — (b)로 이동** |
| `5G NR Beams` 탭 (§3) | SSB 빔 인덱스별 measurement | ⛔ |
| 영상 동기화 재생 (§7) | 주행 영상 소스 자체가 없음 | ⛔ |

> **2026-09-01 정정 — `Missing neighbour`는 데이터 모델이 막고 있지 않았습니다.**
>
> 이 항목은 *"빠진 이웃 = 측정된 이웃 − 설정된 이웃 목록이고 우리는 후자가 없다"*는
> 근거로 여기 올라 있었습니다. **매뉴얼을 확보하고 나니 전제가 틀렸습니다.**
> Nemo는 설정된 이웃 목록을 쓰지 않습니다. UC27(p404)은 **측정값만으로** 판정합니다 —
> *"if Ec/N0 1. best is better than Ec/N0 best active set, the handover has not occurred."*
> 즉 `best active set − 1. best < 0`이면 더 좋은 셀이 있는데 옮겨가지 않은 것이고,
> BLER 상승과 결합해 상태 기계로 격리합니다.
>
> 필요한 두 값 — 서빙 RSRP와 최강 이웃 RSRP — 은 **V7이 이미 넣었습니다.**
> 실제로 `HO_MARGIN` 그래프로 계산해 본 적도 있습니다.
>
> **진짜로 막고 있는 것은 시드 생성기입니다.** 생성기가 매 표본 argmax를 서빙 셀로
> 고르기 때문에 조건이 성립하는 표본이 **정확히 0개**입니다 — 커밋 `9262db0`의 정합성
> 불변식 #2(*"이웃이 서빙보다 강한 표본 = 0"*)가 그것을 단언하고 통과합니다.
> 실제 망은 time-to-trigger와 히스테리시스 때문에 서빙 셀이 일시적으로 열등해지므로,
> 생성기에 그 지연을 넣으면 조건이 자연히 생깁니다.
>
> 이 분류(막힌 원인이 같으면 해결도 같이 된다)는 유효하지만, **어느 분류에 넣을지는
> 근거가 1차 자료여야 한다**는 것이 이번 교훈입니다. 이 항목은 스크린샷 판독에서 나온
> 추론이었고, 매뉴얼이 그것을 뒤집었습니다.

남은 둘은 여전히 화면을 그리는 문제가 아니라 **측정에 없는 값을 어디서 가져올 것인가**의
문제입니다 — SSB 빔 인덱스별 측정과 주행 영상은 우리 데이터에 존재하지 않습니다.

### (b) 실제로 남은 UI·분석 작업

> **2026-09-01 갱신.** 이 표가 쓰인 뒤 UI/UX 백로그 ①–⑧이 전부 진행됐습니다.
> 닫힌 줄은 지우지 않고 취소선과 **무엇이 닫았는지**를 남깁니다.
> 유즈케이스 31개 전부에 대한 현재 상태는 [`docs/use-case-coverage.md`](use-case-coverage.md).

| 항목 | 규모 |
|---|---|
| ~~KPI Workbench **노드 그래프** 빌더 (§7.3)~~ | ✅ **완료.** 캔버스·노드·엣지·CTE 컴파일러 |
| ~~거리 구간 비닝 (§1.2)~~ | ✅ **완료.** 툴바 `Distance bins` → 거리 프로파일 |
| ~~셀 커버리지 폴리곤 (§1.2)~~ | ✅ **완료.** 측정된 서빙 표본의 볼록 껍질. 예측대로 데이터 추가는 필요 없었습니다 |
| ~~사용자 정의 워크북 탭 + 페인별 Layers (§2, §3)~~ | ✅ **완료 — 지도 페인까지.** `+` · 페인 스택 · Layers 체크박스 · 서버 저장. *2026-09-03 정정: 이 칸은 "**단 차트 페인 한정** — MAP 페인은 자기 레이어를 읽지 않습니다"였습니다. 아래 별도 항목이 닫히면서 그 단서도 사실이 아니게 됐는데 이 줄만 남아 있었습니다 — 지도 페인은 자기 트랙을 가져오고, 체크는 배타적입니다.* |
| ~~대시보드 / 추세 분석 (§1.2, §1.3)~~ | **격차 아님 — 취소선 2026-09-03.** 이 칸은 셋 다 `5992-2047` **플라이어** 출처라고 스스로 적으면서 **줄은 열린 채**였습니다. 매뉴얼 근거가 없는 항목은 "아직 안 한 일"이 아니라 **애초에 목록에 없어야 할 일**이고, 이 표의 규칙(닫힌 줄은 지우지 않고 취소선)은 그쪽에도 같이 적용됩니다. 원래 칸 그대로 — **사정 정정 (2026-09-03).** "세션 간 집계 모델 하나에 막혀 있다"는 틀렸습니다. 모델은 P4-3에서 생겼고(코호트), **셋 중 무엇도 열리지 않았습니다** — 셋 다 505쪽 사용자 가이드가 아니라 `5992-2047` 플라이어의 말이고, 그 문서의 "Dashboards (optional)"는 Qlik Sense입니다. 아래 (b-4) 참조 |
| ~~세션 간 집계 모델~~ | ✅ **완료 (P4-3).** 저장된 폴더가 아니라 **차원값**으로 묶고, 그룹마다 **풀링된** 통계 하나(가중 CDF 포함). 아래 (b-4) 참조 |
| 리포트 템플릿 (§1.4) | **미사정** — *2026-09-03 두 번째 정정.* 한 칸에 크기(**중**)와 "미전사, 따라서 **미사정**"이 함께 적혀 있었습니다. 둘 다일 수는 없으므로 크기 칸을 비웁니다 — 사정하지 않았다는 것이 이 줄의 상태입니다. `toc.json`에 10장 Reports가 **p231–318**로 있고(11장 Customization이 p319에서 시작합니다) `Creating and opening a report template`(p235) · `Saving a report template`(p246) · `Configuring reports`(p282)가 목차에 보입니다. **미전사분은 88쪽이 아니라 p231–293과 p311–318, 약 71쪽입니다** — 그 사이 **p294–310은 전사돼 있습니다**(`reference/nemo-analyze-10.2/use-cases.md`의 "10장 — Reports (UC25)" 절: Event Scheduler 배경 p294–307 + UC25 p307–310, `Run Report`·`Run Workbook`·`Run Office report`의 설정 항목까지). 목차가 시사하는 것은 실행 시 섹션을 고르는 피커가 아니라 **설계 시 템플릿 디자이너**이므로, 피커를 이 항목에 대응시키지 마십시오 |
| 페인별 **Tools** 도크 (줌·마커 등) (§2) | ◐ **줌은 완료 (2026-09-04).** 차트가 구간 확대를 받습니다 — 명시적 모드이고(레퍼런스의 `Tool` 메뉴와 같은 모양), 기본 드래그는 커서 스크럽이라 제스처를 빼앗지 않습니다. 기계는 이미 있었습니다: `fromSeq`/`toSeq`가 Problem Survey 맥락 창을 좁히고 있었고 없던 것은 **제스처와 돌아갈 길**뿐이었습니다. 부모가 이미 창을 정한 차트에는 제공하지 않습니다 — 한 도메인에 주인이 둘이면 서로 싸웁니다. 남은 것은 마커 등 나머지 Tools |
| ~~**지도 Layers 목록**~~ (UC13, p150) | ✅ **완료.** 오버레이 아홉 종이 한 목록에 있고, 목록은 **지도에 넘긴 그 객체에서 유도**됩니다(`view/maplayers.ts`) — 무엇을 켰는지의 별도 기록이 아니라 무엇이 그려졌는지에서. 의도에서 만든 목록은 fetch가 한 번 실패하면 어긋나고, 그러면 도크가 없는 레이어를 자신 있게 부릅니다. 검사가 등식을 단언합니다: 도크가 센 수 = 지도가 적은 표본 수(1174 = 1174). 부수적으로 **이벤트와 서빙 셀 선에 처음으로 스위치가 생겼습니다** — 목록에 올려 놓고 보니 끌 수 없는 줄이 둘이었습니다. *2026-09-03 보강: 도크가 **세 지도 탭 모두**(Overview·Mobility·Coverage Issues)에 뜹니다. 전에는 Overview에서만 그려지면서 **공유 상태에 썼고**, 그래서 거기서 끈 스위치가 다른 탭에서 조용히 되살아났습니다. 지금은 화면마다 `mapContents` 객체 하나가 `RouteMap`과 도크에 **함께** 들어갑니다(`App.tsx`) — 손으로 적던 prop 목록은 `maplayers.ts`가 예고한 그대로 어긋나 있었습니다: Mobility와 Coverage Issues는 도크의 스위치가 닿지 않는 서빙 셀 선을 그렸고(`RouteMap`의 `showServingLine` 기본값이 true), 도크가 끈 이벤트를 보여줬고, **툴바의 푸트프린트 셀 필터를 무시**했습니다. 검사도 그 자리로 옮겼습니다 — `verify-scenarios`의 셀 필터 검사가 Coverage Issues에서 `path.footprint-hull.pci-<PCI>`로 **살아남은 셀이 무엇인지**를 셉니다.* 원래 상태 — 중: 레퍼런스는 지도 우측에 `Tools` 아이콘 행 + **레이어 목록**(레이어별 색 아이콘·접기)을 둡니다. 우리 지도 오버레이 9종(경로·이벤트·셀 마커·푸트프린트·모니터드셋 선·영역 빈·거리 빈·폴리곤·공간 차분)은 툴바 셀렉트와 on/off 버튼으로 **흩어져** 조작되고, 지금 무엇이 그려지고 있는지 한자리에서 보여주는 목록이 없습니다 |
| ~~**MAP 페인이 자기 레이어를 읽지 않음**~~ | ✅ **완료.** 지도 페인이 **자기 트랙을 가져옵니다** — 이 파일이 시리즈에 대해 이미 하던 것과 같은 이유로. 그리고 지도는 색 스케일 하나를 칠하므로 지도 페인의 체크는 **배타적**입니다. 원래 상태 — 작음, 결함: `ComposedWorkbook.tsx`가 `visible[0]`을 **캡션에만** 씁니다. RouteMap에는 오버레이 prop이 하나도 넘어가지 않아 두 번째·세 번째 레이어를 체크해도 화면이 그대로입니다. 차트 페인만 `layers`를 실제로 소비합니다 |
| ~~레이어가 **KPI 하나**여서 주행을 레이어로 올릴 수 없음~~ | ✅ **완료 (P3-5).** 레이어가 `{kpiName, visible, sessionId}`이고(`WorkbookService.java` `record Layer`) 세션 id는 `workbook_layer.session_id`(V12)에 저장되므로 **배치의 일부**입니다. *2026-09-03 정정: 이 줄은 완료 뒤에도 열린 채였고, 근거로 달아 둔 `types.ts` · `WorkbookService.java` · `App.tsx`의 줄번호는 **셋 다 다른 것을 가리키게 됐습니다** — 살아 있는 코드를 가리키는 척하는 죽은 인용이야말로 §1.5가 경계하는 모양이라, 사정을 고치면서 함께 지웠습니다. 2026-09-03에 이 문서와 `use-case-coverage.md` 전체에서 같은 처리를 했습니다: 완료된 줄의 "원래 상태" 설명에 달린 줄번호는 **쓰인 시점에는 맞았어도 지금 확인하면 틀리므로**, 파일 이름만 남겼습니다.* |
| ~~Layers 도크의 **레이어 상한 8을 UI가 모름**~~ | ✅ **완료.** `GET /api/workbooks/limits`가 강제하는 쪽에서 숫자를 내주고, 편집기는 그것을 **받아서** 막습니다 — 두 번째 사본이 없으니 어긋날 자리도 없습니다. 원래 상태 — 매우 작음, 결함: 서버는 `MAX_LAYERS_PER_PANE = 8`로 400을 던지는데(`WorkbookService.java`) `+ add layer…` 셀렉트는 9개째도 계속 제시합니다(`ComposedWorkbook.tsx`). 실패가 조작 지점에서 멀리 떨어져 있습니다 |
| ~~**워크북 삭제에 확인이 없음**~~ | ✅ **완료.** 무엇이 사라지는지(페인 수·레이어 수)를 세어 묻습니다. 원래 상태 — 매우 작음, 결함: `remove()`가 곧바로 지웁니다(`ComposedWorkbook.tsx`). 워크북·페인·레이어 전체가 사라지는데, 세션 삭제(`App.tsx`)와 스케일 초기화(`LegendEditor.tsx`)에는 있는 확인이 이것만 없습니다 |
| ~~**범례 격리가 지도 없는 탭에서 거짓말을 함**~~ | ✅ **완료.** 어느 화면이 격리를 반영하는지가 `WORKBOOKS` 표의 한 칸(`isolates`)이 됐고, 범례는 그 칸이 참인 화면에서만 격리를 *제안*합니다. 그리고 같은 스케일로 칠하는 **셀 막대와 거리 프로파일이 이제 격리를 반영**하므로(`view/paint.ts`의 `paintBar`) 화면 넷이 참입니다. 원래 상태 — 매우 작음, 결함: Color Legends 도크는 **탭과 무관하게** 렌더되고(`App.tsx`) `onIsolate`를 항상 넘깁니다. 그런데 `isolate`를 실제로 받는 곳은 RouteMap 셋뿐입니다. 내장 탭 12개 중 지도가 있는 것은 셋이므로, **나머지 아홉 탭에서 구간을 클릭하면** *"the rest is drawn grey for context, not hidden"*(`Panels.tsx`)이라고 적히지만 화면은 그대로입니다 |
| ~~**툴바가 그 화면이 읽지 않는 그룹을 제시함**~~ | ✅ **완료 (2026-09-03).** `WORKBOOKS`에 `isolates` 옆 **`reads` 칸**이 생겨, 각 화면이 다섯 툴바 그룹(`colour` · `areaBins` · `distanceBins` · `area` · `footprints`) 중 무엇을 소비하는지를 **그 화면을 이름 짓는 바로 그 줄에서** 답합니다. 읽는 쪽은 `tabReads` 하나이고, 그래서 화면을 더할 때 두 물음(격리를 반영하는가 · 어느 그룹을 읽는가)에 같은 행에서 답하게 됩니다. 칸으로 둔 이유는 `isolates`와 같습니다 — **범례도 툴바도 화면을 볼 수 없습니다.** 원래 상태 — 매우 작음, 결함: 다섯 그룹이 **열네 탭 전부에** 렌더되고 셋만 소비했습니다. `Ask an area`는 지도 없는 화면에서 `Drawing…`으로 **걸린 채**였고, `Area bins`는 Mobility가 그리지도 않는 타일을 가져왔고, `Distance bins`는 공유 링크에 **죽은 `dist=`**를 썼고(`view/state.ts`), `Colour by: Serving cell (PCI)`는 여전히 KPI로 칠해진 지도 옆에서 **범례만** 바꿨습니다. 제시되고 무시되는 컨트롤은 없는 컨트롤보다 나쁩니다 — 사용자는 데이터가 할 말이 없다고 결론짓습니다 |
| ~~**파일럿 오염에 서빙 품질 조건이 없음**~~ | ✅ **완료** — *2026-09-03 보강: UC20 대화상자의 나머지 두 줄(`Polluter level window from the best active set` = −6, `Pilot count threshold` = 3)도 **화면이 묻습니다.** 둘 다 서버 파라미터로는 있었지만 어느 화면도 보내지 않아 기본값이 곧 도구의 성질처럼 보였고, 패널 설명은 "≥3 셀"을 **고정된 사실로** 적고 있었습니다. 이제 설명은 자기가 보낸 값을 되읽습니다. 실측: 시내 주행에서 3 dB/5 셀은 0구간, 기본값은 1구간, 12 dB/2 셀은 55구간입니다 — 기본값 하나만 보이던 화면이 답하지 못하던 폭입니다.* 단 SINR이 아니라 **RSRQ < −15**로. Ec/N0는 신호를 *총 수신 밀도*로 나눈 값이라 위가 막힌 척도이고 RSRQ가 같은 모양(N×RSRP/RSSI)입니다. SINR은 간섭+잡음 대비라 위가 열려 있고 **수신기 성능을 함께 실어** 옵니다 — 같은 경로·같은 파일럿인데 모뎀이 다르면 판정이 달라집니다. 스키마도 같은 답입니다: `sample_neighbour`에는 셀별 `rsrq`가 있고 SINR은 없습니다. 임계 −15는 우리 카탈로그가 RSRQ를 NORMAL이라 부르기를 멈추는 자리이고, −12를 쓰면 지도는 정상으로 칠하는 표본을 패널이 오염이라 부릅니다. **덤으로 시드 결함 하나**: 프론트홀 세션이 모니터드 셋은 다 쓰면서 `RSRQ`는 한 줄도 안 썼습니다(`DataSeeder`). *2026-09-03 두 번째 보강: 두 입력은 **한 번만** 클램프되고 설명이 **클램프된 값을 되읽습니다**(`MonitoredSetPanel`의 `askedWindowDb`·`askedCells`) — 두 곳에서 클램프하면 보낸 값과 적은 값이 갈라집니다. 그리고 패널은 파라미터가 바뀔 때 자기 데이터를 더 이상 null로 만들지 않습니다. 만들던 동안에는 **타이핑 중인 입력이 언마운트**돼, 창 값을 두 자리로 고치는 것이 한 자리마다 화면이 사라지는 일이었습니다(지금 초기화는 `sessionId`가 바뀔 때뿐입니다).* 원래 상태 — 작음, 정확도: 레퍼런스 UC20의 판정은 조건 **넷**입니다 — 창 `-6` dB · 개수 `3` · RSCP `> -95` · **`Ec/N0 best active set < -12`**. 우리는 앞의 셋만 겁니다(`MonitoredSetService.java` — `rsrp >= best_rsrp - ? AND best_rsrp >= ?`, `HAVING count(*) >= ?`). 서빙 품질이 멀쩡한 곳도 "여러 셀이 비슷하게 강하다"는 이유로 오염이 됩니다. `RSRQ`·`SINR`이 이미 표본별로 있어(`KpiSeed.java`) **데이터는 갖춰져 있고 조건 한 줄**입니다 |
| ~~**푸트프린트를 좁힐 방법이 없음**~~ | ✅ **완료.** *2026-09-03 보강: 좁히는 **입력이 화면에 생겼고**, 문법은 레퍼런스 대화상자가 제 도움말에 적어 둔 그것입니다 — `3,10-30,42,100-`. 열린 범위 `100-`이 이 문법의 값어치입니다: 어디서 끝나는지 모르는 채로 "스몰셀 층"이라고 말하는 방법입니다. 파싱은 `view/pciFilter.ts` 한 곳이고 지도와 설명이 **같은 결과를 읽습니다** — 두 번 파싱하면 셋을 그려 놓고 둘로 좁혔다고 적는 화면이 되고, 그게 이 프로젝트가 계속 찾아낸 결함의 모양입니다. 못 알아들을 항은 **말없이 따르지 않고 거절합니다**(머리글에 `cell filter ignored: …`). 그리고 지도가 무엇을 뺐는지 그림 옆에서 말합니다 — 다섯 중 둘을 그린 지도는 다섯을 그린 지도와 다른 그림인데, 화면의 다른 어떤 것도 어느 쪽인지 알려주지 않습니다.* `pcis`로 좁히고, `basis`로 *어디를 그릴지*를 고릅니다 — 서빙한 곳(기존) 또는 **3강 안에 들었던 곳**(UC1 원문). 후자가 오버슛이 보이는 모양입니다: 이긴 곳만 그리면 멀리까지 닿는 셀이 안 보입니다. 실측 세션 2에서 셀당 표본이 207→613으로 늘고 껍질 면적이 3.3배가 됩니다. **기본값은 서빙 그대로** — 이미 화면과 리포트에 나가 있는 모양의 뜻을 말없이 넓히지 않습니다. 원래 상태 — 작음: `cellFootprints(sessionId, minSamples)`(`GeoAnalysisService.java`)의 유일한 필터가 최소 표본 수입니다. 레퍼런스는 **셀당 한 페이지**로 넘겨 보게 하고 `Scrambling code / Channel number` 필터(`3,10-30,42,100-` 형식)를 겁니다. 우리는 전부 겹쳐 그리는 쪽을 골랐고 중첩이 곧 오염이라는 이점이 있지만, 셀이 수십 개면 **좁힐 수단이 하나도 없습니다** |
| ~~빈을 **어느 통계로 칠할지** 못 고름~~ | ✅ **완료 — 단 "표시만"이 아니었습니다.** 색과 라벨은 서버가 `catalog.binFor`로 만들므로 통계가 **서버까지** 가야 했습니다. 실측: 150 m 격자 39개 중 평균으로는 19개가 녹색인데 **최소값으로는 6개**입니다 — 평균이 가리던 13개가 그 차이입니다. 지도 머리글이 `[Average]`·`[Minimum]`을 적습니다(⑦의 `[Sample]`/`[Distance]`와 같은 규칙). 표준편차·분산·표본수는 **일부러 뺐습니다**: KPI의 임계 사다리는 그 KPI의 단위에 대한 판정이라, dB²짜리 분산을 그 사다리로 칠하면 "신호 세기 규칙으로 고른 빨강"이 "변동이 크다"로 읽힙니다. 원래 상태 — 매우 작음: 레퍼런스는 빈 레이어 `Properties`의 `Statistic`에서 `Average`(기본)·`Minimum`·`Maximum`·`Sample count`·`Std. deviation`·`Variance`·`Mode`를 고릅니다. 우리는 평균 고정입니다 — 다만 `AreaBin`이 이미 `minValue`·`maxValue`·`sampleCount`를 실어 보내므로(`GeoAnalysisService.java`) 셋은 **표시만 바꾸면 됩니다** |
| ~~공간 차분이 **주행 1 대 1**~~ | ✅ **완료.** *2026-09-03 보강: **가까운 쪽도 그룹입니다.** UC16은 `Measurement Group 1`과 `Measurement Group 2`를 **둘 다** 목록으로 두는데, 서비스는 첫 커밋부터 대칭이었고 화면만 아니었습니다 — 그래서 "저녁 주행들 대 아침 주행들"을 한쪽 방향으로만 물을 수 있었습니다. 실측: 시내 주행 하나가 한쪽만 지난 타일 1개인데, 가까운 쪽에 고속 주행을 더하면 104개입니다.* 반대편이 측정 여러 개가 될 수 있고, 그룹 평균은 **풀링**입니다 — 주행별 평균의 평균이 아니라 그룹이 그 타일에 낸 표본 전체의 평균. 둘은 주행마다 표본 수가 다를 때 갈리고(정체를 기어간 주행이 표본이 많습니다), 풀링이 "이 그룹이 여기서 뭘 쟀나"에 답합니다. 양쪽에 같은 측정이 오면 **거부** — 자기 자신과의 차분은 타일을 "일치함"으로 끌어당깁니다. 실측: 시내 두 주행이 한쪽만 지난 타일 1개인데, 고속 주행을 반대편에 더하면 103개. 원래 상태 — 작음: `SpatialDiffService.diff(long sessionA, long sessionB, …)`. 레퍼런스 UC16은 **측정 그룹**(각 그룹 N개)의 평균끼리 뺍니다 — *"A measurement group average is calculated from all measurements within a Measurement Group"* |
| ~~**폴리곤을 필터로** 쓰기~~ | ✅ **완료.** 도형이 통계만이 아니라 **색칠 범위**를 정합니다. 포함 판정은 서버에 남겼습니다 — 브라우저가 도형을 그렸고 좌표도 다 갖고 있지만, ray casting을 TypeScript로 한 번 더 쓰면 "이 표본이 도형 안인가"에 답이 둘이 되고, 그 둘은 통계 패널과 지도 색으로 **나란히 읽힙니다**. 검사가 그 일치를 단언합니다: 트랙이 표시한 안쪽 개수와 통계가 센 표본 수가 같아야 합니다(실측 587 = 587). *2026-09-03 보강: 통계 패널이 **도형을 따라갑니다.** 도형이 끝난 순간 한 번 가져오는 대신 측정·KPI·필터·스케일을 의존성으로 갖는 효과에서 다시 가져오므로(`App.tsx`), 도형을 그려 둔 채 KPI를 바꾸면 지도는 새 KPI로 칠하고 패널은 옛 KPI의 숫자를 들고 있던 자리가 없어졌습니다. `api.areaStatistics`는 폴리곤을 **호출자가 이미 들고 있는 그 전송 문자열 그대로** 받습니다 — 좌표 배열을 두 번째로 직렬화하면 지도가 쓰는 도형과 통계가 쓰는 도형이 둘이 되고, 이 줄이 서버에 포함 판정을 남긴 이유가 정확히 그것입니다.* 원래 상태 — 작음: ⑤가 만든 것은 폴리곤 → **통계**입니다. 레퍼런스 p75–77은 폴리곤 → **필터** — 도형 안 구간만 값 색으로 칠하고 나머지는 기본색으로 둡니다. 도형과 포함 판정(`AreaSelection`)은 이미 있으므로 남은 것은 그 술어를 색칠 경로에 거는 일입니다 |
| ~~지도 우클릭 → **셀 ID로 전역 필터** (UC14 p149)~~ | ✅ **완료 (2026-09-04).** 우클릭에 버튼 하나짜리 팝업 — 우클릭 자체로 거는 것이 아니라 묻습니다. 필터는 화면 전체를 다시 좁히므로 붐비는 지도에서 사고로 발동할 조작입니다. **덤으로 기능보다 값진 결함 하나**: 셀 사이트가 **자기 중심점에서 클릭이 안 됐습니다.** 모든 레이어가 Leaflet 기본 오버레이 페인을 공유해 쌓임이 DOM 순서(=마지막에 돈 이펙트)였고, 경로는 KPI·색·격리가 바뀔 때마다 다시 그려져 마커 위에 얹혔습니다. 남은 건 방위각 스포크가 삼켰고요 — 핸들러도 없는 장식인데 사이트 중심에서 시작합니다. 사이트 전용 페인 + 스포크 비대화형. **줄곧 있던 PCI 툴팁도 이제야 닿습니다** |
| ~~**Exclude Events** (p94)~~ | ✅ **완료 (2026-09-04).** 문법에 `notevent:TYPE`이 생겼습니다. `GlobalFilter`의 주석이 *"둘이 하나를 공유할 수 있을 때까지 기다린다"*고 적어 둔 항목이었고 — `network_event`는 ts만 있고 seq가 없어 표본으로 해석해야 하는데 그 규칙이 `AnalysisService`에 따로 있었습니다 — 이제 `EventOnSample`이 그 규칙을 갖고 **셋이 바인딩합니다**(이벤트 목록·전역 필터·Problem Survey). **±5표본 창은 우리가 더한 것이고 필터 설명이 그 숫자를 말합니다**: 매뉴얼은 실패한 *호*를 지우는데 우리에겐 호 경계가 없고, 표본 하나를 빼는 건 어떤 백분위도 바꾸지 않습니다. 실측 1200 → 1178표본, **P05가 −115.5 → −114.1** — 나쁜 호 하나가 백분위에 앉아 있던 게 이 기능의 요점입니다. 진입점은 Events 목록(이벤트가 망 탓이 아니라고 판단하는 자리)이고, 이미 걸린 조건을 **대체하지 않고 더합니다** |
| ~~셀 ID로 **전역 필터 만들기** · 이벤트 제외 (묶음)~~ | ✅ **기계 부분 완료 (P3-2).** *2026-09-03: 이 줄의 **큼**은 "모든 조작에 걸리는 필터 기계"를 사정한 것이고, 그 기계는 나왔습니다(14개 엔드포인트가 지키고 8개가 이유와 함께 면제). 남은 둘은 위 두 줄로 쪼갰습니다.* 원래 사정 — 큼 — *2026-09-02 재사정(중 → 큼).* 레퍼런스의 전역 필터는 *"applied to all operations"*인데, 우리 백엔드는 세션 표본을 **57곳**에서 읽고 엔드포인트가 **43개**입니다. 일부에만 걸면 "모든 조작에 걸린다"는 화면이 어떤 화면에서는 무시된다는 뜻이라, P0에서 여섯 개 없앤 것과 **같은 종류의 거짓말**이 됩니다. 세션 범위를 정하는 자리를 하나로 모으는 선행 작업이 이 항목의 실제 크기입니다. 원래 설명 — 중: 지도에서 섹터 우클릭 → `Create Global Filter From Cell ID` → 이후 모든 질의가 그 셀이 서빙하던 지점만. `Exclude Events`도 같은 자리(측정 오류로 실패한 호 빼기). **전역 필터** 항목의 구체적 진입점 둘입니다 |
| ~~KPI 실행 시 **묻는 변수** `{?이름}`~~ | ✅ **완료 (2026-09-04).** *한 번 "안 만든다"고 적었고 그 논거가 틀렸습니다* — "발행된 그래프는 이름 하나에 물질화되므로 복제가 정답"이라고 적었는데, 이름 하나에 스냅샷 하나뿐이라는 사실은 **복제가 대안이 아니라 변수가 막는 피해**라는 뜻입니다. 똑같이 시작해 갈라지는 두 문서가 가장 나쁜 상태이고 그게 제품에 있던 상태였습니다. 값은 **요청에** 실리고 문서에 저장되지 않습니다. **숫자만, 이스케이프가 아니라 거부**: `ColumnCondition`의 안전 논거가 "사용자 입력의 어떤 조각도 SQL에 닿지 않는다"인데 변수는 표현식 *안에* 값을 넣는 유일한 자리라, 맨 숫자면 컴파일러가 숫자 리터럴로 파싱해 자기 것을 씁니다. 실측: 한 그래프가 −100에서 83표본, −110에서 36표본 |
| 이름 붙은 **재사용 색상셋**(자산) | 큼 — *2026-09-03 재정정.* **이 줄에 있던 "색상셋 `string` 타입 — 작음"은 반대 방향으로 틀렸습니다.** `string`은 **P3-3에서 나왔습니다**: 이름별 색은 `event_type` 레지스트리이고, 그것을 **사용자가 편집할 수 있게** 만든 것이 `EventTypeCatalog.recolour` + `PUT /api/event-types/{name}/color`(`AnalysisController`) + Events 도크의 `EventColourEditor`(`Panels.tsx`)이며, V12가 같은 마이그레이션에서 `scale_type`과 나란히 `event_type.color_overridden`을 넣어 **재시드가 사람의 선택을 되가져가지 않게** 했습니다. `verify-ui`가 그 색이 **지도 마커와 도크에 함께** 옮겨 가고 새로고침 뒤에도 남는 것을 단언합니다. 앞선 "정정"이 든 근거 — *"V12의 `scale_type` CHECK가 받는 값은 둘뿐"* — 은 **그 마이그레이션 파일 자신이 반박합니다**: 같은 파일이 `string` 색상셋은 숫자 범위가 없어 `kpi_threshold`의 경계에 살 수 없고 **그것이 이름 붙이는 대상**(=`event_type`)에 속한다고 적어 놓았습니다. `scale_type`에 없는 것은 누락이 아니라 **설계**였고, CHECK를 근거로 삼은 것은 스키마의 한 자리만 보고 기능의 부재를 단언한 것입니다. 그래서 세 타입은 다 있고, **진짜로 남은 것은 이름 붙은 재사용 색상셋이라는 자산**입니다 — 이름·그룹·타입·서버 업로드·`.csf`/`.aex` 반출입이고 그래서 **큼**이며, 그것이 이 줄입니다. 원래 설명 — *2026-09-02 분리*: 우선순위표에서 "이름 붙은 재사용"과 한 줄에 묶여 있었는데 크기가 다릅니다. 레퍼런스 색상셋은 `numerical` · **`gradient`**(연속) · **`string`**(문자열별 색) 세 타입입니다(p427–443) |
| ~~Activity 탭~~ / **Log window** 탭 (§2) | 작음 — **Activity는 ⑧에서 완료**(진행 중인 잡 · 읽은 행 수 · 개별 취소). **전역 필터 표시도 P3-2에서 나왔습니다** — 이 줄에 남아 있던 것은 2026-09-03에 지웠습니다. *2026-09-03 보강: 취소가 **약속한 답을 돌려주게 된 것도 이날입니다.** `ImportService.ImportStopped`의 주석은 쓰인 날부터 "so the API can answer 409, not 400"이라고 적고 있었는데 **핸들러가 없어** 진짜 임포트를 취소하면 500이 났고, 그 경로를 지나간 검사도 없었습니다. 이제 `ApiExceptionHandler`에 `@ExceptionHandler(ImportService.ImportStopped.class)`가 있습니다. 레퍼런스의 `Cancel All`은 없고, 임포트가 순차라 `RUNNING`은 많아야 하나입니다 — §2의 갈라진 두 줄 참조.* **Log window(p213)만 남습니다** |
| ~~`5G Physical Layer` 탭 (§3)~~ | ✅ **가능 — 격차 아님 (2026-09-03).** KPI가 이미 있습니다(`KpiSeed`의 **Link Adaptation** 카테고리 — `DL_BLER` · `CQI` · `PDSCH_MCS` · `PDSCH_RANK`, 넷 다 임계 사다리까지 시드됨). 그리고 사용자가 **지금 그 화면을 직접 만들 수 있습니다** — 탭 스트립의 `+` → `+ Chart pane` → Layers 도크에서 넷을 올리면 그것이 `5G Physical Layer` 탭입니다. 내장 탭으로 미리 만들어 두지 않은 것은 배치의 선택이지 없는 기능이 아닙니다 |
| RACH `maximum preamble`·`preamble response` 2필드 (§3) | 작음 — *2026-09-03 출처 표시.* **매뉴얼 근거가 없습니다.** 두 이름은 505쪽 사용자 가이드 전사본 어디에도 없고(`docs/reference/nemo-analyze-10.2/` 전체 검색 0건), 출처는 §1의 기준 이미지 `nemo-outdoor_5g-nr_main-window`, 즉 **Nemo Outdoor** 화면입니다. Outdoor는 *수집* 도구이고 Analyze가 아닙니다. 줄은 남깁니다 — §3의 도크가 그 화면을 본떠 만들어졌으므로 19개 중 17개라는 셈은 계속 맞아야 합니다 — 다만 이것은 **Analyze 대비 격차가 아닙니다** |
| ~~**`Missing handover` / `Missing neighbour` 원인**~~ | ✅ **완료 (2026-09-04).** 막고 있던 이유가 **두 번째**였습니다 — 기록된 첫 이유("설정된 이웃 목록이 없다")는 이미 정정돼 있었고, 아무도 찾지 않은 진짜 이유는 생성기가 매 표본 argmax를 서빙으로 골라 **"이웃이 서빙보다 강한" 표본이 21,070행 중 0개**였다는 것입니다. 검출기를 먼저 썼다면 영원히 0건인 파이 조각이 생겼을 겁니다. 생성기에 **지속시간형** 핸드오버 지연(구간이 아니라 — 구간은 크로스오버 위치에 달려 62건↔0건으로 흔들리고 마진이 43 dB까지 갔습니다)과 **스트랜디드 셀** 하나. 검출: 최강 이웃이 서빙보다 3 dB 이상(A3 히스테리시스) 4표본 이상. **두 원인을 가르는 추론은 우리 것**입니다 — 레퍼런스는 둘을 이름으로만 부르고 구분법을 적지 않았으며, 설정 목록이 없으니 증거는 주행뿐입니다: 다른 데서 서빙하는 셀이면 늦은 것, 어디서도 안 하면 관계가 없는 것. 실측 늦은 핸드오버 5건(4.9–10.7 dB) · 관계 없음 1건(32.1 dB, 45표본) |
| ~~드릴다운 **다중 탭**(좌측, 조각 색) (§4)~~ | ✅ **완료 (2026-09-03).** 여는 순서대로 탭이 쌓이고, 탭 색은 파이 조각 색을 **그 조각에서 가져옵니다** — 색을 두 번 정하면 탭과 조각이 갈라지고, 그러면 탭 색이 가리키는 곳이 없어집니다. 이미 활성인 원인을 다시 누르면 닫히므로 단일 슬롯 시절의 되돌아가기도 그대로 있고, 탭을 닫으면 **남아 있는 탭으로 물러납니다** — 읽던 사람이 닫은 것은 탭 하나이지 비교 자체가 아닙니다. 검사는 탭 수와 **서로 다른 색 두 개**를 세고, 그 다음 탭을 눌러 **밑의 사례 목록이 바뀌는지**까지 봅니다 — 탭이 그려지기만 하고 고르지 않으면 두 원인을 여는 뜻이 없습니다. 원래 사정 — **중** — *작음에서 상향.* 스타일이 아니라 여러 원인을 동시에 열어 두는 기능(p88) |
| ~~**워크벤치 State machine이 레퍼런스와 다른 것**~~ | ✅ **완료 (P4-1).** `KpiGraph.ladder()`가 점유마다 한 행을 내고 값이 머문 시간(ms)입니다 — 아래 (b-3) 참조. 원래 사정 — 큼 — *중에서 상향.* 래칭만의 문제가 아닙니다. 레퍼런스 출력은 **상태 점유마다 한 행 + `time_interval`(머문 시간 ms)** 이고(p370), 그래서 **절차 지연 측정**이라는 용도가 성립합니다. 우리 것은 표본별 `CASE` — 정직한 이름은 **분류기(Classifier)** 이고 진짜 State Machine은 별도 작업입니다 |
| ~~**Previous / Current / Next Value 상관 노드**~~ | ✅ **완료 (2026-09-02).** 다섯 형태(previous · current · next · previous-or-current · next-or-current)와 **primary 게이팅**까지. primary는 레퍼런스처럼 *"가장 왼쪽 입력"* 이 아니라 **셀렉트**입니다 — 컴파일러가 좌표를 읽지 않고(같은 그림 → 같은 SQL을 위해 입력을 노드 번호로 정렬), 아무도 강제하지 않는 배치 규약보다 말로 적는 쪽이 작습니다. 창(window)은 **두 입력의 합집합** 위에서 돌고 게이트는 **맨 마지막에** 걸립니다 — 먼저 걸면 "직전"이 *"직전 이벤트의 값"* 이 되고, 그건 다른 질문에 대한 그럴듯한 답입니다. `Within (ms)`는 **우리가 더한 것**이고 그렇게 적혀 있습니다 |
| ~~**Cell locator**~~ | ✅ **완료 (2026-09-04).** *"정답이 있으니 데모"라던 사정이 거꾸로였고 원문 그림이 그렇게 말합니다* — p175가 **실제는 초록, 추정은 보라**로 한 지도에 그립니다. 기록을 갖고 있는 것이 이 기능의 **사용 조건**입니다: 셀 DB는 이설·급전 교체·좌표 오기입으로 낡고, 추정과 기록이 300 m 어긋나면 의심할 것은 기록입니다. **추정기는 측정으로 골랐고 교과서 답이 졌습니다** — 경로손실 피팅은 모든 분위에서 더 나빴고(중앙값 193 m 대 67 m, 3.7 km 이상치) 이유는 RSRP가 거리만의 함수가 아니기 때문입니다(섹터 로브가 방위에 따라 최대 20 dB). 최종은 최강 8 dB 이내 전력가중 센트로이드, **중앙값 54 m**. **방위각은 보고하지 않습니다** — 두 번 만들어 재봤고 정답 위치를 쥐여줘도 중앙값 30°라 한계가 기하입니다. confidence는 **실제 오차와 상관이 있는 것**으로 만들었습니다(처음 쓴 분산×표본수는 −0.27/−0.02로 무의미했고, 근접도가 −0.83). 검사는 공식이 아니라 **주장**을 겁니다 — "6 이상이면 100 m 이내", 레퍼런스가 자기 점수에 붙인 그 정확도 |
| ~~푸트프린트 포함 기준을 **3강 안에 든 셀**로~~ | ✅ **완료.** `basis=TOP3`. 순위 컬럼은 없고 `row_number() OVER (… ORDER BY rsrp DESC) <= 3`로 유도합니다 — V7이 측정된 것만 저장하고 나머지는 유도한다는 원칙 그대로 |
| 거리 빈에 **40λ 옵션** | 작음 — **2026-09-02 보류로 판정.** n78 ARFCN 633984 = 3509.76 MHz이므로 λ = 8.54 cm, **40λ = 3.42 m**입니다. 1 Hz에서 표본 간격은 보행 5 km/h에 1.39 m, 시내 주행 3.67 m, 고속 27.8 m — 빈당 표본이 각각 2.5개 · 0.93개 · 0.12개입니다. **보행 데이터셋에서는 빈이 비지 않습니다**(주행만 보고 "빈 화면이 나온다"고 적었던 첫 판단은 정정). 다만 Lee's criteria의 나머지 절반은 **창 안에 표본 36~50개**이고 보행에서도 2.5개라, 1 Hz로는 어느 속도에서도 채워지지 않습니다 — 레퍼런스가 이 항목을 스캐너 쪽에 둔 이유입니다. 넣는다면 밴드에서 λ를 유도하고(ARFCN→주파수 변환은 코드에 **0건**) **빈당 표본 수를 화면이 말하게** 해야 하며, 다중 밴드 세션은 λ가 하나로 정해지지 않아 밴드 필터와 짝지어야 합니다(원문도 *"when running a query for the band"*) |
| 세션이 **보행인지 주행인지** 판정 | 중 — **2026-09-02 신규, 보류.** `sample.speed_kmh`가 이미 있으므로 중앙 속도로 갈립니다(보행 ~1.4 m/s 대 시내 ~8 m/s). UI와 문서가 전부 "drive"·"주행"이라 부르는데, 보행 데이터셋이 들어오면 화면이 매번 조금씩 틀린 말을 하게 됩니다 — P0에서 여섯 개 고친 것과 같은 종류입니다. 40λ의 적정 빈 크기도 여기서 유도됩니다 |
| 파라미터 **★ 즐겨찾기**, 사용자 KPI의 **출처 표시** | 작음 — *신규.* 레퍼런스는 사용자 KPI를 `User` 가지에 모읍니다(p83). 우리는 카테고리를 사용자가 입력해 기본 KPI와 섞입니다 |
| ~~임포트 **진행 큐 + 취소**~~, KPI 그래프 **생성 SQL 표시** | ✅ **진행 큐·취소는 ⑧에서 완료** — `POST /api/import/jobs/{id}/cancel`이 요청을 남기면 적재 루프가 다음 배치에서 보고 멈추고, **실패와 같은 롤백**을 타므로 절반 남은 세션이 생기지 않습니다. SQL 표시는 **하지 않기로 판정** — 그 밑의 요구는 ⑥의 노드 미리보기가 더 잘 답합니다 |
| ~~**거리 가중 통계** — `statistics` · `distribution` · CDF · 리포트~~ | ✅ **⑦에서 완료.** 가중치는 `RouteContinuity.travelledMetres`에서 오고, 화면에는 `[Distance]`·`[Sample]`이라는 **이름**으로 나옵니다. dB 파라미터의 선형 평균도 같은 자리(`AggregationBasis`)에서 골라집니다 |
| 기성 워크북을 **복사해 고치기**, 레이아웃 프리셋 | 작음 — *신규.* 내장 탭의 지식을 사용자 출발점으로(p215) |
| ~~**워크벤치가 상태 이름을 범례에 넘기지 않음**~~ | ✅ **문구를 내렸습니다** — 화면(`KpiWorkbench.tsx`)과 **컴파일러 주석(`KpiGraph.java`)이 둘 다** 같은 거짓을 적고 있었습니다. 이제 상태 이름은 캔버스에 머물고, 발행된 상태 기계는 1·2·3으로 그려진다고 말합니다. 참으로 만드는 길은 값싸지만(상태는 연속 정수이고 KPI 임계 사다리가 이미 값 구간에 이름을 붙입니다) **결정 하나가 먼저**입니다 — 그래프가 저장될 때마다 사용자가 편집한 스케일을 덮어써도 되는가. 그 결정 전까지는 문구가 사실을 말합니다 |

### (b-1) 우선순위 — 2026-09-02

(b)는 목록이고 순서가 없습니다. 원문 전사판 대조가 끝난 지금 시점의 순서를 적습니다.
기준은 **"도구를 믿을 수 있는가 → 조작이 정직한가 → 값에 비해 싼가 → 진짜 기능 → 결정이 먼저"**
입니다. 기능 개수가 아니라 **신뢰**를 앞에 둔 이유는, 없는 기능은 사용자가 알지만 **틀린 답은
모른다**는 것 하나입니다.

| 순위 | 항목 | 크기 | 왜 이 자리인가 |
|---|---|---|---|
| ~~**P0-1**~~ | 파일럿 오염에 **서빙 품질 조건** 추가 | 작음 | ✅ 완료. `MonitoredSetService`가 서빙 품질을 함께 겁니다 — `POLLUTION_MAX_SERVING_RSRQ_DB = -15.0`, KPI는 `RSRQ`. SINR이 아닌 이유와 −15인 이유는 위 §6(b)의 해당 줄에 |
| ~~**P0-2**~~ | 범례 격리 알림이 **지도 없는 탭에서 거짓** | 매우 작음 | ✅ 완료. `WORKBOOKS`의 `isolates` 칸이 참인 화면에서만 범례가 격리를 제안합니다(`App.tsx`의 `tabIsolates` → `LegendPanel`의 `onIsolate`). `verify-ui`가 Overview에서는 구간에 손가락 커서가 뜨고 Statistics에서는 안 뜨는 것을 단언합니다 |
| ~~**P0-3**~~ | 워크벤치가 **상태 이름을 범례에 넘긴다는 문구** | 작음 | ✅ 완료. 화면과 컴파일러 주석 양쪽에서 내렸습니다. 지금은 발행된 분류기가 **1, 2, 3…으로 그려진다**고 적고, 이름은 캔버스에 머뭅니다(`KpiWorkbench.tsx`) |
| ~~**P1-1**~~ | MAP 페인의 **Layers 도크 무력** | 작음 | ✅ 완료. 지도 페인이 **자기 트랙을 가져오고** 체크된 레이어가 `RouteMap`으로 넘어갑니다(`ComposedWorkbook.tsx`). 지도는 스케일 하나를 칠하므로 지도 페인의 체크는 배타적입니다 |
| ~~**P1-2**~~ | 레이어 **상한 8을 UI가 모름** | 매우 작음 | ✅ 완료. `GET /api/workbooks/limits`가 강제하는 쪽에서 숫자를 내주고 편집기가 그것을 **받아서** 막습니다 — 셀렉트가 비활성되고 "이 페인은 N개로 가득 찼습니다"라고 적습니다 |
| ~~**P1-3**~~ | **워크북 삭제에 확인 없음** | 매우 작음 | ✅ 완료. `remove()`가 사라질 **페인 수와 레이어 수를 세어** 묻습니다(`ComposedWorkbook.tsx`) |
| ~~**P2-1**~~ | 빈을 **어느 통계로 칠할지** | 작음 | ✅ 완료. **"응답에 이미 있으니 표시만"은 틀렸습니다** — 색을 서버가 만들므로 통계도 서버까지 갑니다 |
| ~~**P2-2**~~ | **푸트프린트 필터**(PCI 목록) | 작음 | ✅ 완료 |
| ~~**P2-3**~~ | **폴리곤을 필터로** | 작음 | ✅ 완료 |
| ~~**P2-4**~~ | 푸트프린트 **3강 기준** | 작음 | ✅ 완료. 40λ는 보류(위 (b) 참조), 보행/주행 판정도 보류 |
| ~~**P3-1**~~ | **지도 Layers 목록** | 중 | ✅ 완료 |
| ~~**P3-2**~~ | **전역 필터** | **큼** | ✅ 완료. **14개 엔드포인트가 지키고 8개가 면제**이며(P4-3에서 코호트 화면이 더해졌습니다), 면제는 목록에 이유와 함께 실려 화면에서 읽힙니다. 아래 (b-2) 참조 |
| ~~**P3-3**~~ | 색상셋 **gradient · string** 타입 | 중 | ✅ 완료 — **둘 다.** *2026-09-03, 정정의 정정: 이 칸은 원래 "gradient · string 완료"라고 **맞게** 적혀 있었는데, 같은 날 `string`은 안 나왔다며 틀린 쪽으로 고쳤습니다. 근거였던 "V12의 `scale_type` CHECK가 받는 값은 둘뿐"은 근거가 아닙니다 — **같은 마이그레이션 파일**이 `string` 색상셋은 숫자 범위가 없어 `kpi_threshold`에 살 수 없고 `event_type`에 산다고 적고, 거기에 `color_overridden`을 넣습니다. 편집 경로는 `EventTypeCatalog.recolour` · `PUT /api/event-types/{name}/color` · `EventColourEditor`(`Panels.tsx`) 셋이고, `verify-ui`가 색이 지도와 도크에 함께 옮겨 가는 것을 단언합니다.* 이름 붙은 재사용 색상셋은 **별도 큼 항목**입니다(위 §6(b)) |
| ~~**P3-4**~~ | 공간 차분 **측정 그룹** | 중 | ✅ 완료. 레이어 = 주행은 **별도 큼 항목**으로 분리 |
| ~~**P3-5**~~ | 레이어 = **주행** | **큼** | ✅ 완료. `workbook_layer.session_id`(V12)로 저장되므로 **저장된 배치의 일부**이지 이번 화면의 상태가 아닙니다 |
| ~~**P4-1**~~ | **State Machine 구간 출력**(`start_time`·`time_interval`) | 큼 | ✅ 완료 — **저장 모델은 건드리지 않았습니다.** 아래 (b-3) 참조 |
| ~~**P4-2**~~ | **Previous / Current / Next** 상관 | 중 | ✅ 완료. 다섯 형태 + primary를 **셀렉트로** 지정 + 우리가 더한 `Within (ms)` 경계 |
| ~~**P4-3**~~ | **세션 간 집계 모델** | 큼 | ✅ 완료. **폴더가 아니라 차원값**으로 묶습니다. 아래 (b-4) 참조 |

**P0–P1 여섯 개가 전부 닫혔습니다.** *2026-09-03 정정: 이 자리에 "여섯 개를 합쳐도
작습니다"가 현재형으로 남아 있어, 아래 두 줄 뒤에 "모두 닫혔습니다"를 적어 놓고도 여섯 개를
여전히 열린 일감처럼 읽게 만들고 있었습니다.* 합쳐도 작다는 **사정 자체는 맞았습니다**: 전부
이미 있는 데이터와 이미 있는 화면 위의 일이었고, 넷은 "코드가 말과 다르다"를 고치는
것이었습니다.

**2026-09-03 현재 P0·P1·P2·P3·P4가 모두 닫혔습니다.**

### (b-4) 세션 간 집계 — 폴더를 짓지 않았습니다 — 2026-09-03

P4-3의 명백한 모양은 **폴더**입니다. 레퍼런스가 가진 것이 그것이고("Folders", 그리고 델타
플로팅의 Measurement Group 둘), 우리도 `measurement_folder` 테이블 하나면 됐습니다.
**세 가지 이유로 버렸습니다.**

1. `measurement_session`이 이미 아는 사실의 **두 번째 집**이 됩니다. "어느 주행이 1.5.0인가"에
   답이 둘이 되고, 누군가 한쪽을 고치는 순간 갈라집니다.
2. 주행을 지웠을 때의 **수선 규칙**이 필요해집니다 — 멤버만 뺄지, 그룹째 지울지, 거부할지.
   그 규칙이 사는 곳이 하필 `view/state.ts`, 즉 "주행별 인덱스는 다른 데서 아무 뜻이 없다"를
   중심 논지로 삼는 파일입니다.
3. 코호트를 가리키는 **링크가 세션 id를 실어야** 합니다. 그 파일이 존재하는 이유가 정확히
   그것을 조심하기 위해서입니다.

차원값은 셋 다 없습니다. 주행 하나를 지우면 버킷의 개수가 하나 줄고, 그게 전부입니다.
`?by=BUILD_LABEL&hold=SCENARIO` 링크는 **받는 쪽 서버의 1.5.0 주행들**을 가리킵니다.

**왜 한 번의 풀링 질의이고 N개의 합성이 아닌가.** 개수·최소·최대·가중합은 세션별 부분합에서
정확히 합쳐집니다. **백분위는 안 됩니다** — 어떤 가중이든 그룹의 중앙값은 멤버들의 중앙값에서
복원되지 않습니다. 클라이언트에서 세션별 `Statistics`를 합쳤다면 **맞는 평균 옆에 없거나
지어낸 CDF**를 실어 보냈을 것입니다. 그래서 `WeightedStats.computeAcross(SessionSet, …)`가
진짜 구현이고 한 세션짜리가 그리로 위임합니다 — 이 응용에 평균은 여전히 **하나**입니다.

**교란 가드, 그리고 무엇을 뺐는지 이름으로 말하는 것.** "1.5.0이 1.4.2보다 나은가"는 두 주행
집합이 그 밖으로는 비교 가능할 때에만 빌드에 관한 물음입니다. 두 번째 차원을 고정하면
**모든 버킷에 나타나는 값**의 주행만 남고, 빠진 것은 **전부 이름과 이유로** 나열됩니다 —
개수는 독자가 검증할 수 없는 숫자이고, 이 응용의 규칙은 "좁힌 것은 무엇을 뺐는지 말한다"입니다.
고정 차원이 없으면 **델타는 있고 판정은 없습니다**. 가드 없는 "낫다"는 빌드만큼이나 도로를
재기 때문입니다.

| | 레퍼런스 | 우리 |
|---|---|---|
| 묶는 방식 | Folder (저장된 멤버십) | **차원값** (`build_label`·`scenario`·`device`·`operator`·`technology`·`location_name`) |
| 델타 플로팅 | Measurement Group **정확히 둘** | 코호트 **최대 8** (팔레트가 여덟이고 그 이상은 범례가 안 읽힙니다) |
| 그룹 통계 | Group By / Aggregate | 풀링된 한 번의 질의 — **가중 CDF까지** |
| 교란 | 없음 | **hold-constant**, 그리고 제외된 주행을 이름으로 |
| 판정 | 없음 | 고정 차원이 있을 때만 BETTER/WORSE, 없으면 델타만 |
| 시간축 | 리포트 스코프 | **없음** — 세로는 순서일 뿐이고 차트가 그렇게 적습니다 |

**대시보드·NPS·추세 리포트는 여전히 없습니다 — 그리고 이제 그것이 P4-3에 막혀 있지
않다는 것이 분명해졌습니다.** 셋 다 505쪽 **사용자 가이드에 없는** 말입니다. 출처는
`5992-2047` **플라이어**이고([`keysight-vdt-research.md` §출처표](keysight-vdt-research.md)),
그 문서의 "Dashboards (optional)"는 Qlik Sense를 가리킵니다. 위 §6 표에서 "대시보드·추세가
전부 이것 하나에 매여 있다"고 사정했던 것은 **플라이어를 매뉴얼처럼 읽은 결과**였습니다.
집계 모델이 생긴 지금 그 셋 중 무엇도 저절로 열리지 않았고, 그것이 그 사정이 틀렸다는
증거입니다 — §8이 기록한 것과 같은 오류의 되풀이입니다.

**절대 자르지 않은 한 줄**: 세로 간격은 시간이 아닙니다. 행은 각 코호트의 **첫 주행 순서**로
놓이고 간격은 일정하므로, 1년 떨어져 시험된 두 빌드가 하루 떨어진 둘과 똑같이 한 행 간격입니다.
그래서 두 평균을 잇는 선은 **직각 꺾은선**이고 대각선이 아닙니다 — 대각선은 그 사이에서 값이
매끄럽게 움직였다는 주장인데, 그 사이에서는 아무것도 측정되지 않았습니다. 차트 하단이 그렇게
적습니다.

### (b-3) 구간 출력에 새 저장 모양이 필요하지 않았습니다 — 2026-09-02

P4-1을 "저장 모델을 건드리는 유일한 항목"으로 사정한 것은 **틀렸습니다.** 레퍼런스의 출력
격자는 `start_time | end_time | time_interval` 세 열이지만,
`end_time = start_time + time_interval`이므로 **자유도는 둘**입니다. 그리고 `sample_kpi` 행에는
이미 둘이 있습니다 — `ts`와 `value`.

그래서 상태에 **진입한 표본에** 머문 시간을 찍습니다. 그러면 `ts`가 곧 원문의 `start_time`이
되고(매뉴얼이 커스텀 이벤트 생성을 설명할 때 지목하는 바로 그 시각입니다), 구간 하나가
평범한 KPI 행 하나에 **손실 없이** 들어갑니다. 결과: 새 테이블도, 두 번째 결과 모양도,
`recompute()`의 분기도, 두 번째 미리보기 경로도 없고, **이미 있는 화면 전부가** 그 값을
칠하고 비닝하고 필터링하고 내보내고 리포트에 싣습니다.

| | 레퍼런스 | 우리 |
|---|---|---|
| 상태 순서 | 전이 그래프 | **사다리** — 상태 k는 k−1에서만 진입 |
| 기억 | 있음 (두 가지 일: 순서 + 스트림이 흩은 값 모으기) | 있음 (**순서만**. 값 모으기는 스키마가 이미 함 — kpi-workbench.md §7.3) |
| 출력 | 점유마다 한 행 + `time_interval` | 점유마다 한 행. `ts`=시작, `value`=ms |
| Time trigger | 있음 | **없음** — 조건 문법에 부정이 없습니다. 화면이 그렇게 적습니다 |
| 임의 전이표 | 있음 | **없음** — 순차 접기라 `WITH RECURSIVE`가 필요하고, 그것을 매 임포트마다 전 주행에 돌립니다. 화면이 그렇게 적습니다 |

**이름을 바꿨습니다.** 표본별 `CASE`는 `Classifier`가 됐고(정직한 이름이라고 우리 문서가
이미 적어 둔 그 이름), `State machine`이 사다리를 가리킵니다. 저장된 문서에는 그 문자열이
들어 있으므로 **V13이 데이터까지 고칩니다** — 안 그러면 저장된 그래프가 200을 돌려주며
상태 코드 1·2·3을 밀리초로 조용히 바꿔 놓습니다. `spec.version`이 붙은 이유도 하나뿐입니다:
옛 문서를 **새 것으로 읽지 않고 거부**하기 위해서.

**절대 자르지 않은 한 줄**: 구간은 그 안에서 **측정이 끊긴 적이 없을 때만** 발행됩니다.
표본별 끊김 카운터를 주행의 `sample`에서 읽어 진입과 종료에서 같은지 봅니다. 실측 —
시내 1.4.2 주행의 seq 540 페이드는 seq 560의 26표본 GPS 공백을 건너뛰므로 **발행되지
않습니다**. 가드를 빼면 그 자리에 `64000 ms`라는 완벽하게 그럴듯한 값이 나타납니다.
같은 조건의 1.5.0 주행(공백 없음)은 `71000 ms`로 정상 발행됩니다.

### (b-2) 전역 필터가 실제로 미치는 범위 — 2026-09-02

P3-2를 "큼"으로 사정한 이유가 그대로 설계가 됐습니다. **어디까지 미치는지를 코드가 스스로
말하게** 만들었습니다 — `GlobalFilter.coverage()`가 목록이고, `GET /api/global-filter/coverage`가
그 목록이며, 화면의 **Reach: 14 of 24** 버튼이 같은 목록을 펼칩니다.

| 지킴 (15) | 면제 (14) · 이유 |
|---|---|
| `track` · `series` · `distribution` · `statistics` · `cell-breakdown` · `degradations` · `area-statistics` · `bins` · `cell-footprints` · `export.csv` · `export.geojson` · `report.html` · **`cohorts`** · **`distance-bins`**(2026-09-03) · **`serving-lines`**(2026-09-04) | `events` · `messages` — **시각(ts)으로 매인 행**이라 표본 필터가 고르지 못합니다 |
| | `snapshot` — 표본 하나를 번호로 지목 |
| | `monitored-set` · `pilot-pollution` · `neighbour-breakdown` — 이웃 행 / 구간 판정. 가운데 표본을 빼면 **한 구간이 두 개로 보고**됩니다 |
| | `spatial-diff` · `compare` — **아직 필터 파라미터를 받지 않습니다.** 배선이 안 됐을 뿐 불가능한 것이 아닙니다 |
| | **`cell-locator`**(2026-09-04) — confidence 1–10이 **주행 전체의 증거로 보정**돼 있어, 조건으로 표본을 덜어 낸 위치는 같은 점수를 주장할 수 없습니다. 조건이 걸린 화면에서 내보낸 파일은 행마다 `not applied`라고 적습니다 |
| | **`cells`** · **`field-to-lab`** · **`{id}`**(2026-09-04) — 셀 참조 행 / 주행 전체로 제안하는 채널 모델 / 측정이 **기록한** 것. 셋 다 표본 조건이 바꿀 대상이 아닙니다 |

> **2026-09-04 — 이 표에 없던 넷을 찾은 방법.** `cell-locator` · `cells` · `field-to-lab`는
> **지킴에도 면제에도 없었습니다.** 목록에 없는 경로는 "전부 두 번 호출해 좁아지는지 본다"는
> 그 단계가 아예 부르지 않으므로, **조건을 무시하면서 모든 검사를 통과**합니다 —
> `distance-bins`가 실제로 그렇게 배포됐던 경로입니다. 이제 `api-surface.mjs`가 이 목록을
> 자기가 이미 뽑아 둔 매핑에 대고 재고, 어느 쪽에도 없는 세션 분석이 하나라도 있으면
> 빨개집니다(§1.5.14 — 감사하는 목록은 자기 완전성을 **다른 곳에서** 재야 합니다).
| | `coverage-issues` · `problem-survey` — **아직 필터 파라미터를 받지 않습니다**(2026-09-03 추가). `coverage-issues`는 표본을 하나씩 분류하므로 `/degradations`가 지키는 것과 같은 모양이고, `problem-survey`는 원인이 **검출기 셋과 이벤트 스트림 하나**에서 오므로 배선하려면 이벤트에서 온 원인에 조건이 무슨 뜻인지를 먼저 정해야 합니다 — `/events`가 면제인 바로 그 물음입니다 |

구현은 **`(session_id, seq) IN (...)` 하위질의 한 줄**입니다. 이 스키마의 모든 분석이 그 쌍으로
매여 있으므로, 그 절은 무엇을 select·join·group by 하든 **끼워 넣기만 하면** 됩니다. 조인이나
컬럼별 WHERE 조각이었다면 질의마다 다른 모양이 되고, 그러면 "이 엔드포인트가 필터를 지키는가"를
**기계가 물을 수 없습니다.**

> **2026-09-03 두 번째 정정 — 목록에 아예 없던 경로가 하나 있었습니다.** `/distance-bins`는
> `filter` 파라미터를 받지 않았고 **지킴에도 면제에도 없었습니다.** 이 목록을 읽는 검사(S20)는
> "지키는 경로가 조건을 실었나 / 면제 경로가 안 실었나"를 보므로, **어느 쪽에도 없는 경로는
> 무엇을 하든 통과합니다.** `cell:210`을 걸면 여섯 화면이 좁아지는 옆에서 거리 프로파일만 전
> 주행을 그렸고, 면제라는 표시도 없었습니다. 지금은 지킴 목록에 있고 S20의 한-숫자 축약에
> 열 번째 질의로 들어갑니다(전체 1174 / 필터 558). 축은 그대로입니다 — 4500 m 도로는 양쪽 다
> 4500 m이고, 표본 없는 빈만 목록에서 빠집니다(18 → 17).
>
> **교훈은 목록이 아니라 목록의 모양에 있습니다**: 항목이 빠진 감사 목록은 틀린 항목보다
> 발견하기 어렵습니다. 틀린 항목은 검사가 잡지만, 없는 항목은 검사할 대상 자체가 없습니다.

> **2026-09-03 세 번째 정정 — 목록에 없던 경로가 둘 더 있었습니다.** `/coverage-issues`와
> `/problem-survey`는 `distance-bins`와 **같은 모양의 구멍**이었습니다: 지킴에도 면제에도 없어
> S20이 무엇을 하든 통과했고, 화면에도 면제라는 표시가 없었습니다. 지금은 둘 다 이유와 함께
> 면제로 실려 있고(`GlobalFilter.coverage()`), 목록은 **14 지킴 + 10 면제 = 24**입니다.
> `distance-bins` 때 적은 교훈이 그대로 다시 적용됩니다 — 빠진 항목은 틀린 항목보다 찾기
> 어렵습니다. 다른 점은 이번에는 **이유가 "아직 안 함"**이라는 것입니다: `coverage-issues`는
> `/degradations`와 같은 표본 단위 분류라 배선만 남았고, `problem-survey`는 배선 전에 결정이
> 하나 필요합니다.

> **2026-09-03 정정.** 원래 키는 `seq` 하나였고, `spatial-diff`·`compare`의 면제 사유는
> *"필터는 한 주행의 표본을 지목합니다"* 였습니다. P4-3에서 키가 `(session_id, seq)` 쌍이 되면서
> **그 사유가 사실이 아니게 됐습니다.** 사유가 낡은 목록은 목록이 막으려던 바로 그 결함이므로,
> 키를 바꾼 같은 커밋에서 두 줄을 고쳤습니다 — 지금 사유는 "안 됨"이 아니라 **"아직 안 함"**입니다.

`report.html`은 필터를 **적용하고 또 적어 냅니다**. 리포트는 그것을 만든 화면에서 가장 멀리
떨어져 읽히는 산출물이고, 조건을 적지 않은 부분집합 통계표는 전체 주행의 통계표와 구별되지
않습니다.

검사는 `verify-scenarios` S20 **스물다섯 걸음**입니다(*2026-09-03 재확인: 스물한 걸음이라고 적혀
있었습니다 — 세는 것은 `step(` 호출이고 S20 블록에 지금 25개가 있습니다*). 핵심은 **아홉 개
질의의 표본 수를 집합으로 줄이는 것** — 필터 없이 전부 1174, 있으면 전부 558이고, 거리
프로파일이 **같은 두 숫자에 합류하는지**를 따로 봅니다. 자세한 것은
`docs/ui-testing/README.md` §1.5.10.

### (c) 의도적으로 범위 밖

| 항목 | 이유 |
|---|---|
| 3D Visualizer (§1.2) | 실내 층별 시각화 — 우리 대상은 야외 주행 |
| 트랜스포트 컨트롤 record/pause/stop (§3) | 우리는 사후 분석 전용. 실시간 수집을 하지 않음 |
| 리본 UI (File/View/Tools/…) (§2) | 의도적 단순화 |
| Keysight/Nemo 로고·워드마크·브랜드 레드 | `docs/assets/NOTICE.md` — 복제 금지 |


---

## 7. 2026-08-31 보강 — 실제 VDT UI를 뒤늦게 확보

§5를 쓸 때 근거로 삼은 `s8709a-vdt_fig3-single-interface.png`는 **UI가 아니라 블록 다이어그램**이었습니다
(현재는 `s8709a-vdt_fig2-equipment-chain.png`로 개명). **진짜 Figure 3**은 S8709A 기술개요 5페이지에
있었고, 이전 추출 스크립트의 픽셀 임계값에 걸려 누락돼 있었습니다. 이번에 추출했습니다.

| 화면 | 실제 구성 | 우리 상태 |
|---|---|---|
| **`FIELD LOGS PROCESSING`** | 로그 메타(제품·측정일시·소요·거리·평균속도), UE 데이터(제조사·모델·펌웨어·칩셋), 검출 캐리어 표(Link/Technology/Cell ID/Frequency/Band), 경로 지도, DUT 측정 셀 파워 차트, **Extracted channel model** 상태, `Generate simulation` 버튼 | ✅ *2026-09-03 정정 — 이 줄은 §7.3이 **완료**로 적은 뒤에도 ⛔로 남아 있었습니다.* `Field-to-Lab` 탭(`App.tsx`의 `WORKBOOKS`)이 로그 메타·경로·셀 파워·추출된 채널 모델을 한 화면에 놓습니다. **UE 데이터의 칩셋·펌웨어·SW 빌드는 일부러 비어 있습니다** — 우리가 기록하지 않는 항목이라 그럴듯한 값으로 채우지 않았습니다 |
| **`RUN VIEW`** | Project/Campaign 드롭다운, `Run`/`Cancel Test Case`, **Duration·Progress·Pass Rate 게이지 3개**, 테스트 케이스 그리드(시작시각·진행바·상태·판정·리포트), `Result KPI` 표(Measured value / Comparison operator / Expected value) | ◐ 판정 표는 **대응물 있음**(Acceptance criteria: KPI/Aggregate/Condition/Actual/Result). *2026-09-03 정정: **게이지 3종과 Run/Cancel도 나왔습니다**(§7.3이 완료로 적고 있고 `BringUpPanel`이 합격률 게이지를 그립니다) — 이 칸의 ⛔는 낡은 것이었습니다.* 남은 차이는 Project/Campaign 드롭다운과 테스트 케이스별 리포트 링크입니다 |
| **`TEST EXECUTION MONITORING`** | 주행 시점 영상 + 게이지 오버레이 + 경로 지도 인셋 + 좌우 차트 카드 열 + 타임라인 커서 | ◐ 공유 시간 커서·지도·차트는 있으나 **영상 동기화 없음** |

### 7.1 셀 상태 스트립 — 접속 상태의 표준 관용구

S8709A 문서에는 셋업·연결 절차가 **한 줄도 없습니다.** 그래서 S8709A가 자기 구성요소로 지목한
UXM 5G의 문서(`S8711A`)를 확보했고, 거기서 접속 상태를 어떻게 보여주는지 확인했습니다.

```
L1 │ PCC / FDD    │ n78 │ -60   dBm/15kHz │ BW  10 MHz │ D/U 18300  │ CONNECTED
L2 │ SCC / FDD    │ n78 │       dBm/15kHz │ BW 300     │ D/U 18300  │ OFF
N1 │ NSA PCC/TDD  │ n78 │ -19.85 dBm/BW   │ BW 100 MHz │ D/U 623334 │ CONNECTED
N2 │ SA PCC / TDD │ n78 │ -19.85 dBm/BW   │ BW 100 MHz │ D/U 623334 │ OFF
```

우측 세로 액션은 `Main` / `Cell Off` / `RRC Release` / `Power Control` / `CA/HO` / `Blind Handover`,
하단 탭은 `System` / `Scheduling` / `Cell` / `PHY` / `MAC·RLC·PDCP` / `RRC·NAS` / `UE Info`입니다.

**우리 상태**: *2026-09-03 정정 — §7.3이 이 항목을 **완료**로 적은 뒤에도 아래 문장이 그대로 남아 있었습니다.* 셀 상태 스트립은 나왔습니다. 아래는 그것이 없던 시절의 서술이고, 남은 차이(우측 세로 액션과 하단 탭 일곱)를 읽는 데만 씁니다. 원래 서술: 장비 체인은 있으나 **셀 단위 상태 스트립이 없습니다.** 브링업 시퀀스가 "cell started"를
한 줄로 말할 뿐, 어느 셀이 몇 MHz로 어느 ARFCN에서 붙어 있는지는 보이지 않습니다.
`S8708A`는 같은 것을 `UE Attached` / `Calibration` 두 램프(회색→녹색)로 표시합니다.

> **정직성 경계**: `S8711A`/`S8708A`는 S8709A **자체가 아니라 같은 계열 형제 툴셋**의 문서입니다.
> S8709A가 UXM 5G를 구성요소로 명시하므로 그 UI가 랙 안에 있다는 것은 확실하지만, S8709A 운용자가
> 저 화면을 직접 조작하는지는 공개 문서로 확인되지 않았습니다.

### 7.2 문제 조사 — 2026년 판으로 갱신

`nemo-analyze_problem-survey-drilldown_1836x1123.png`(2026 판)가 §4의 2020년 UMTS 판을 대체합니다.
원인 분류가 LTE/5G 세대로 바뀌었습니다 — `Dropped RRC connection` 2.44% / `File transfer dropped`
82.93% / `Handover failure` 9.76% / `Data server connect failure` 4.88%. 드릴다운 그리드는
**`Handover type`(LTE FDD 1800 → LTE FDD 1800)과 `HOF cause`** 열을 갖고, 최종 워크북은 RSRP 트레이스
위에 **핸드오버 마커**를 찍고 `PDN CONNECTIVITY REQUEST`를 IE 단위로 디코드해 보여줍니다.

### 7.3 우선순위 갱신

| 순위 | 항목 | 근거 |
|---|---|---|
| ~~P0~~ | ~~셀 상태 스트립~~ | §7.1 — **완료** |
| ~~P0~~ | ~~바 차트~~ | §2 — **완료** (`Cells` 탭) |
| ~~P0~~ | ~~Run/Cancel + Duration·Progress·Pass Rate 게이지~~ | §7 — **완료** |
| ~~P1~~ | ~~원인 분류 → 파이 → 드릴다운~~ | §7.2 — **완료** (`Problem Survey` 탭) |
| ~~P1~~ | ~~필드→랩 변환 화면~~ | §7 — **완료** (`Field-to-Lab` 탭). 단 UE 데이터의 칩셋·펌웨어·SW 빌드는 **우리가 기록하지 않는 항목**이라 그럴듯한 값으로 채우지 않고 아예 빼놓았습니다 |
| ~~P1~~ | ~~KPI Workbench — **노드 그래프** 빌더~~ | **완료** (`Import` 화면). 레퍼런스 워크벤치 스크린샷을 직접 판독해 설계했고 — **그 "스크린샷"의 정체는 매뉴얼 UC27의 완성 노드 그래프였습니다**(p425와 같은 내용, 다른 크롭. 처음에 "p405 상태 흐름도"라고 적었던 것은 크기 일치만 보고 내린 오판이라 철회합니다 — §8.1) — 결정적 단서는 Output 노드의 `Column count: 19`였습니다 — 그래프는 **값이 아니라 이름 붙은 열을 가진 행 집합**의 데이터플로라는 뜻이고, 그래서 수식으로는 표현할 수 없습니다. 정렬 노드는 의도적으로 없습니다 |
| ~~P0~~ | ~~이웃 셀(monitored set) 데이터 모델~~ | **완료** (V7). 막혀 있던 네 기능 중 셋이 함께 열렸습니다 |
| **P2** | 영상 동기화 재생 | §7 `TEST EXECUTION MONITORING` — 데이터 소스가 없어 보류 |

> **합격률 게이지의 규칙**: 아직 평가되지 않은 런은 `n/a`로 표시합니다. 판정을 받지 않은 런은
> 기준을 통과하지 못한 것이 아니라 아직 판단되지 않은 것이고, 0%로 쓰면 정반대로 읽힙니다.

---

## 8. 2026-09-01 보강 — 사용자 가이드 505페이지 확보

이 문서의 §1 "방법"은 **매뉴얼 기재 항목이 아니라 화면 이미지를 근거로 삼았다**고
밝히고 있습니다. 그것이 당시로서는 옳은 선택이었습니다 — 그때 가진 매뉴얼이 마케팅용
Technical Overview 12종이었고, 12개 중 11개가 **번호 붙은 절차 0건**이었기 때문입니다.
기능 목록만 있고 조작이 없는 자료로는 UI를 대조할 수 없었습니다.

**이번에 진짜 사용자 가이드가 들어왔습니다** — `NTN00000A-90013`, Edition 1, 2023-11-27,
505페이지. 각 메뉴의 설명, 입력·출력, 그리고 **Use Case 31개**가 실려 있습니다.
정리는 두 갈래로 나눴습니다.

| 위치 | 용도 |
|---|---|
| [`docs/reference/nemo-analyze-10.2/`](reference/nemo-analyze-10.2/) | **구현 작업용.** 목차·Use Case 인덱스(기계 판독), 노드 요소 전체 명세, 화면 인벤토리, 정정 목록 |
| [`docs/briefs/`](briefs/) | **분석 자료(HTML).** 메뉴 설명 + 유저 시나리오 + 우리 구현에 어떻게 반영됐는가 |

### 8.1 화면 이미지로는 알 수 없었던 것

이번에 뒤집힌 판단들에는 공통점이 있습니다. **틀린 것은 전부 "화면에 보이지 않는
것"이었습니다.**

| 판단 | 화면에서 읽을 수 있었나 | 결과 |
|---|---|---|
| 노드 그래프의 **구조** (소스→결합→연산→상태→출력) | 보임 | ✅ 정확했음 |
| 노드 **배치·연결 방향·상태 이름** | 보임 | ✅ 정확했음 |
| 빨강/초록 **색 규약** | 보임 | ✅ 정확했음 |
| State Machine의 **출력 모양** | **안 보임** | ❌ 틀림 (구간 출력 + `time_interval`) |
| Combine의 **게이팅 규칙** | **안 보임** | ❌ 틀림 (primary 개념) |
| `Missing neighbour`의 **판정 방식** | **안 보임** | ❌ 틀림 (측정값만으로 판정) |
| Lee's criteria의 **실제 수치** | **안 보임** | ❌ 틀림 (40λ) |
| 드릴다운 좌측 탭의 **의미** | 탭은 보였으나 **용도는 안 보임** | ❌ 규모 오판 |

노드가 화면 어디에 놓이는지는 그림에 있지만, 그 노드가 **무엇을 출력하는지**는 그림에
없습니다. 스크린샷 기반 분석의 한계가 정확히 거기였습니다.

### 8.2 매뉴얼을 읽고 **하지 않기로** 한 것

빼는 근거가 "귀찮아서"가 아니라 **우리 데이터 모델에서 의미가 없어서**라는 점이
중요합니다. 매뉴얼이 세 곳(p353·p375·p378)에서 반복하는 문장이 근거입니다 —
*"the Nemo measurement file format is **time-based** as opposed to sample-based (a 'sample' is
created on a timeline only when **changes occur**)"*.

| 항목 | 왜 하지 않는가 |
|---|---|
| 집계의 **`Weight by <ins>time</ins>`** | 시간 가중이 필요한 이유는 Nemo 표본의 **길이가 제각각**이기 때문입니다. 우리는 1 Hz 균일이라 **가중 없는 평균이 맞습니다.** 넣으면 의미 없는 컨트롤이 됩니다 |
| **Time: Resample** | 같은 이유. 재표본화할 불규칙성이 없습니다 |
| **Sort 노드** | 정렬이 필요한 이유가 Union의 순서 파괴인데(p359) 우리는 Union이 없고 `seq` 키입니다 |
| 드릴다운 **더블클릭** 진입 | 한 번 클릭이 낫다고 판단합니다. 고칠 것은 **동시 유지** 쪽입니다 |
| MapX · Spreadsheet grid · Info views · Query clipboard | MapInfo 자산 계열이거나 우리가 다루지 않는 데이터 종류 — **범위 차이지 격차가 아닙니다** |
| 그래프 **좌/우 이중 축** (UC6 p108–109) | *2026-09-03 — §6(b)에서 옮겼습니다.* **이 표의 머리말과는 다른 종류의 이유이므로 한 마디가 필요합니다**: 위 다섯은 "우리 데이터 모델에서 의미가 없어서"이고, 이것은 **의미가 있는데 대안이 더 나빠서**입니다. 결정은 이미 두 곳에 발행돼 있었습니다 — `ComposedWorkbook.tsx`(`MultiSeriesChart` 머리 주석)와 [`user-guide.md`](user-guide.md) §3.4: *"두 번째 축을 다는 대안은 더 나쁩니다: 교차점이 의미 있는 것처럼 읽히지만 사실은 두 축을 따로 고른 결과일 뿐입니다."* 한 페인에 **여러 계열은 이미 됩니다.** 각 계열은 자기 범위로 정규화되고 축에는 숫자가 없으며 실제 범위는 범례에 숫자로 적힙니다 — 계열이 하나면 그 단위의 진짜 눈금이 붙습니다. 그러므로 이것은 **남은 작업이 아니라 내린 결정**이고, §6(b)에 열린 줄로 두는 것은 두 문서가 반대로 적는 상태였습니다. (`use-case-coverage.md`의 전사 격차 #9가 아직 §6(b)를 가리킵니다 — 그 파일을 다음에 만질 때 여기로 고쳐야 합니다) |

> **2026-09-01 정정 — 이 표에서 한 줄을 뺐습니다.** 원래 `Weight by`가 통째로 여기
> 있었습니다. **거리 가중은 여기 있으면 안 됩니다.** Appendix 3(p477)이 거리 가중의 근거를
> 표본 간격과 **무관하게** 댑니다 — *"the weighting effect of time spent at traffic lights is
> to be excluded, the samples should be weighted by distance."* 그리고 1 Hz 균일 샘플에서
> 정차 편향은 **가장 심합니다**(정차 중에도 초당 1표본, 이동 0 m). Appendix 6에는
> `QSR_DISTANCE`와 `QSR_TIME`이 별개 프로시저로 존재합니다(p495–497).
> 거리 가중 통계는 **실제로 남은 작업**이며 §6(b)로 옮겼습니다.


### 8.3 매뉴얼이 확인해 준 것

| 판단 | 매뉴얼 |
|---|---|
| 정렬 노드를 넣지 않음 | **근거까지 확인** — 정렬이 필요한 이유는 Union이 순서를 파괴하기 때문(p359) |
| 파일럿 오염에 하한 조건(`best RSRP ≥ −110`) | ~~우리가 더 나음~~ → **같은 설계, 다른 값.** *2026-09-02 철회* — 원문 p173의 필터에 `RSCP active set best above threshold = -95`가 있습니다. 하한이 필요하다는 판단은 맞았고, "레퍼런스엔 없다"는 근거가 틀렸습니다. **그리고 반대 방향의 격차가 드러났습니다** — 레퍼런스는 `Ec/N0 active set best < -12`도 함께 걸어 *서빙 품질이 실제로 나쁠 때만* 오염으로 봅니다. 우리 질의에는 그 조건이 없습니다(아래 (b)) |
| 필터를 텍스트 조건식으로 | **우리가 더 나음** — 레퍼런스는 이진 트리라 한 노드에 자식이 둘뿐(UC26, p396) |
| 세로형 `sample_kpi` 스키마 | **구조적 우위** — 매뉴얼이 밝힌 워크벤치의 존재 이유 중 하나가 *"셋 이상 테이블의 시간 상관이 impossible"*(p332)인데, 우리는 같은 키의 조인입니다 |
