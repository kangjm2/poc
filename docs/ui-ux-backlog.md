# UI/UX 백로그 — 순서와 근거

`docs/gap-analysis.md`와 `docs/ui-gap-vs-reference.md`가 **무엇이 다른가**를 적는 문서라면,
이 문서는 **무엇을 어떤 순서로 할 것인가**입니다. 레퍼런스와의 격차 중 **화면에 닿는 것만**
추렸고, 각 항목을 실제 코드에 대조해 확인한 뒤 순서를 매겼습니다.

## 어떻게 만들었나

| | |
|---|---|
| 후보 | 매뉴얼 정밀 조사에서 나온 검증된 발견 94건 중 UI에 닿는 것 |
| 확인 | 34개 주장을 **프론트엔드 코드에 직접 대조** — 29건 부재 확인, 5건 "부분적으로 있음", 0건 오검출 |
| 순위 | 서로 다른 세 사용자 관점이 **독립적으로** 전체 순위를 매김 |
| 종합 | 세 순위를 평균내지 않고, **갈린 지점은 "어느 작업 방식에 필요한 기능인가"로** 판정 |

세 관점은 이렇습니다.

- **베테랑** — Nemo 8년. 이 도구로 갈아타라는 말을 들었고, 클릭 한 번이 아깝습니다.
- **신입** — Nemo를 모릅니다. 뭐가 없는지 모르니, *막히지 않는가*와 *내가 틀렸을 때 알 수 있는가*만 봅니다.
- **장애추적** — "이 구간에서 전화가 끊긴다"는 민원 하나를 오늘 안에 규명해야 합니다.

---

## 0. 먼저 — 확인 과정이 제 주장 셋을 고쳤습니다

백로그를 쓰기 전에, 제가 앞서 적었던 것 중 **틀린 것**이 나왔습니다.

| 제가 적었던 것 | 실제 |
|---|---|
| "재생 기능이 없다" | **있습니다.** `App.tsx:88, 203–214`에 재생 루프가 있고 상태 바에 ▶/⏸가 있습니다. 진짜 문제는 다른 데 있습니다 — 아래 |
| "새로고침하면 아무것도 남지 않는다" | **과했습니다.** 사용자가 조립한 워크북은 **의도적으로 서버에 저장**되고 실제로 살아남습니다(`V9` 주석과 `verify-ui.mjs:643`이 그걸 단언합니다). 사라지는 것은 **뷰 상태**입니다 |
| "단축키가 하나도 없다" | 앱 코드에는 없지만 **Leaflet 자체 키보드 핸들러는 켜져 있습니다** |

### 재생의 진짜 결함은 "없음"이 아니라 **해상도**입니다

```js
const stepSize = Math.max(1, Math.round(maxSeq / 240))   // App.tsx:207
```

재생은 주행 길이와 무관하게 **항상 240스텝**입니다. 1분 안에 전체를 훑겠다는 의도는
합리적이지만, 결과적으로 **2시간 주행(약 7,200표본)에서는 30표본씩 건너뜁니다.**
30개 중 29개에는 커서가 **영원히 닿지 않습니다.**

> 1 Hz 균일 샘플이라 "다음 표본"이 정확히 정의되는데도 그렇습니다.
> **모델의 한계가 아니라 UI의 양자화 선택**이고, 그래서 고칠 수 있습니다.

---

## 1. 순서

### ① 증거 무결성 — 도구가 **틀린 답을 자신 있게** 내놓는 곳 `s` — ✅ **완료 (2026-09-01)**

> **결과**: 단 하나의 잘못된 GPS 고정이 4.4 km 주행을 **17.82 km로** 보고하고 있었습니다 —
> 실제의 **4배**. 세 화면(지도 · 거리 프로파일 · Field-to-Lab)이 각자 거리를 계산하고 있었고,
> 그중 **셋 다** 이 값을 그대로 썼습니다.
>
> 규칙을 `RouteContinuity` 한 곳에 두고 세 소비자가 공유하게 했습니다. 지도가 "여긴 측정
> 안 했다"고 그리는 구간을 거리 축이 여전히 주행거리로 세고 있으면, 두 화면이 서로 다른
> 주행을 말하면서 아무도 눈치채지 못합니다.
>
> | | 이전 | 이후 |
> |---|---|---|
> | 세션 1 주행거리 | 17.82 km | **4.40 km** |
> | 같은 경로 세션 2 | 4.50 km | 4.50 km |
> | 지도 | 터널을 직선으로 이어 그림 | 결측 구간은 **회색 점선**, 거부된 고정은 **빨간 점선** |
>
> **검증**: 91/91 · 97/97 · api-surface 무결점. 새 검사 5개 전부 **결함 주입으로 실패를
> 증명**했습니다(항목별로 자기 검사만 실패). 검사 자체의 결함도 둘 잡았습니다 —
> `.leaflet-container`는 이 빌드에 아예 없어서 healthy 코드에서 실패하고 있었고,
> 프레이밍 지표를 `min`으로 잡아 패널의 종횡비를 재고 있었습니다.
>
> **부수 발견**: 검사의 `waitUntil: 'networkidle'`이 차단된 타일 때문에 간헐적으로
> 타임아웃 — `domcontentloaded`로 교체했습니다(스킬 문서가 경고하던 바로 그것).

세 관점이 **거의 같은 문장으로** 지목한 유일한 항목입니다.
베테랑 *"내가 거짓을 발표하게 만드는 유일한 항목"*, 신입 *"내가 틀렸다는 걸 알려주지 않는 유일한 구멍"*,
장애추적 *"느린 답이 아니라 틀린 답을 주는 유일한 항목"*.

| 항목 | 지금 무슨 일이 일어나나 |
|---|---|
| **경로선이 결측 구간을 잇습니다** | 90초 터널 구간이 건물 사이를 지나는 **직선 한 줄**로 그려지고, 툴팁도 평범한 커버리지처럼 붙습니다 → **차가 측정한 적 없는 길에 커버리지 홀을 표시하게 됩니다** |
| **GPS 튐에 방어가 없습니다** | 잘못된 좌표 하나가 바다까지 갔다 오는 스파이크를 그리고 **주행 거리에 1 km를 조용히 더합니다** |
| **임포트가 중복 이름을 막지 않습니다** | `drive_0412`가 두 개 생기고, 두 주 뒤에 **엉뚱한 주행을 10분간 분석**하게 됩니다 |

> 뒤에 오는 **모든 거리 기반 통계의 전제**이기도 합니다. 가짜 1 km가 들어간 거리 값 위에
> 거리 가중 평균을 얹으면 오염이 전파됩니다.

### ② 이벤트의 시각 채널 `s`~`m` — 순위 1 / 4 / 1 — ✅ **완료 (2026-09-01)**

> **결과**: 네트워크가 보고한 이벤트는 **표의 행으로만** 존재했습니다. 지도에도 차트에도
> 없었고, 사건을 클릭하면 **아무 화면도 표시하지 않는 커서**가 움직였습니다.
>
> 타입·표시명·색·심볼을 `event_type` 테이블(V10) 한 곳에 두고, 지도 마커 · 차트 눈금 ·
> Events 독 · 문제 파이가 **모두 같은 레지스트리**를 읽게 했습니다. 색만으로는 복사본이나
> 색각 이상 사용자에게 전달되지 않으므로 **글리프가 정체성을 지고 색이 보강**합니다.
>
> | | 이전 | 이후 |
> |---|---|---|
> | 지도 | 이벤트 없음 | 타입별 **심볼 마커** 15개 |
> | 차트 | 이벤트 없음 | 시각 위치에 **점선 + 글리프** 15개 |
> | Events 독 | `RADIO_LINK_FAILURE` | **Radio link failure** (파이·지도와 같은 단어) |
> | 사건 클릭 | 보이지 않는 커서만 이동 | 전후 30표본 **맥락 창**이 열림 |
>
> **검증**: 95/95 · 106/106 · api-surface 무결점 · 백엔드 25/25. 새 검사는 결함 주입
> 4건으로 실패를 증명했고, **항목별로 자기 검사만** 실패했습니다(지도를 죽이면 차트 검사는
> 초록으로 남습니다 — 세 채널이 서로를 대신 통과시켜 주지 않는다는 뜻).
>
> **검사 자체의 결함을 하나 잡았습니다.** "맥락 창이 주행 전체가 아닌 창인가"를 **캡션**에서
> 읽고 있었는데, 캡션은 `CONTEXT_PAD` 산술이라 차트가 창을 받았든 아니든 같은 문장을
> 인쇄합니다. 창 prop을 `null`로 만든 빌드에서 차트는 주행 전체를 그리는데 검사는 그대로
> **통과**했습니다. 차트의 **x축 눈금**(그려진 점에서 뽑은 시각)을 재도록 바꾸자 1199s /
> 1174표본으로 실패합니다.
>
> > **①에서 배운 것의 재확인**: 파생값(캡션)을 재면 원본(차트)이 어긋나도 모릅니다.
> > ①에서는 공유 규칙이 소비자들을 *틀린 채로 서로 동의하게* 만들었고, 여기서는 캡션이
> > *차트를 보지 않고* 옳은 문장을 인쇄했습니다. 같은 실수의 두 얼굴입니다.

**아무도 이 클러스터에서 무엇 하나 미루지 않았습니다.** 더 결정적인 것은,
세 관점이 **서로 다른 항목**을 각자의 최우선으로 꼽았다는 점입니다 — 지도 마커, 차트 주석,
전후 맥락 창. 하나짜리 기능이 아니라 **한 능력을 세 각도에서 본 것**이므로 통째로 내야 합니다.

1. ✅ **이벤트 타입 레지스트리** `m` — `event_type`(V10) 9행. `EventTypeCatalog`가 캐시하고,
   모르는 타입은 **버리지 않고** 회색 `?`로 세웁니다 — 임포트가 새 타입을 들고 오면 측정이
   실제보다 조용해 보이는 쪽이 더 나쁩니다
2. ✅ **지도에 타입별 심볼** `s` — `divIcon` 마커. 경로선·셀·빈·푸트프린트를 세는 기존 검사
   4개의 숫자를 건드리지 않으려고 **path가 아닌 div**로 그립니다
3. ✅ **차트에 이벤트 마커** `s` — 그려진 도메인 안의 이벤트만. 밖의 것은 프레임 가장자리에
   달라붙어 *경계에서 일어난 사건*처럼 읽힙니다
4. ✅ **드릴다운에 전후 맥락 창** `m` — 분석 화면과 **같은 차트 컴포넌트**를 창만 씌워 재사용.
   두 번째 구현은 seq→x 매핑이 어긋날 두 번째 자리입니다

### ③ 기본 조작 표면 `s` — 순위 4 / 2 / 2, 장애추적의 최악 항목 — ✅ **완료 (2026-09-01)**

> **설계를 18개 에이전트로 먼저 돌렸습니다.** 7개가 각자 다른 각도로 현재 코드를 읽고,
> 4개가 독립 설계를 내고, 3개가 서로 다른 렌즈로 채점하고, 4개가 우승안을 깨려고 했습니다.
> **심사 셋이 서로 다른 안을 뽑았습니다**(회귀 위험→최소 diff, 항목 종결→발견가능성,
> 일관성→URL 권위). 우승자가 없다는 것이 답이었고, 합성이 답이 됐습니다.
>
> **깨기 단계가 blocker 5건을 냈고, 그중 셋이 설계를 바꿨습니다.** 두 에이전트는 실제로
> 임시 브랜치에 구현해 놓고 **측정**했습니다.
>
> | 발견 | 무엇이 문제였나 |
> |---|---|
> | Leaflet이 화살표를 삼킴 | `_onKeyDown`이 `stop(e)`를 부릅니다. 지도를 클릭하면 표본 커서가 **이유 없이 죽습니다**. 실제로 제 첫 검사가 이걸로 실패했습니다 |
> | 경로 bbox는 뷰포트의 증인이 아님 | Leaflet이 화면 밖 폴리라인을 `d="M0 0"`으로 잘라내고 리마운트마다 다시 자릅니다. 뷰포트가 **바이트 단위로 동일한데도** bbox가 99 px 움직인 것을 측정했습니다 |
> | 죽은 세션 링크 | URL의 `s`가 지운 세션이면 **다른 주행**이 열리고 보낸 사람의 `seq`가 거기 적용됩니다. 화면은 완벽히 자기모순 없이 **틀린 순간**을 그립니다 — ①이 없애려던 바로 그 부류 |
>
> **③a로 넣은 것** (커서 · 키맵 · Esc · 지도 프레이밍):
>
> | | 이전 | 이후 |
> |---|---|---|
> | 재생 스텝 | `round(maxSeq/240)` — 7200표본에서 **30씩** | **항상 1표본**, 속도는 틱 주기가 담당 |
> | 커서 기록자 | 15곳이 각자, 클램프 없음 | `moveCursor` **하나**가 클램프 |
> | 커서당 요청 | 이동마다 2건, 취소 없음 | 125 ms 디바운스 + 양쪽 `live` — **초당 8건 상한 유지** |
> | 키보드 | **하나도 없음** | 17개 바인딩, `keymap.ts` 한 테이블이 **바인딩과 도움말을 동시에** 생성 |
> | Esc | 없음 | 우선순위 사다리 하나 |
> | 지도 | KPI만 바꿔도 프레임 리셋 | 주행이 바뀔 때만. `F`로 되돌림 |
>
> **검증**: 102/102 · 106/106 · api-surface 무결점. 새 검사 7개 전부 결함 주입으로 실패를
> 증명했고 **항목별로 자기 검사만** 실패했습니다.
>
> **주입이 중복 코드를 하나 잡았습니다.** `useDismissable`이 App과 LegendEditor **양쪽**에
> 등록되어 있어서, 어느 쪽을 꺼도 모달이 닫혔습니다. 둘 다 "동작한다"고 보이지만 **어느
> 것도 일하고 있음을 증명할 수 없는** 상태였습니다. App 하나만 남겼습니다.
>
> **③b로 넣은 것** (URL 뷰 상태):
>
> `view/state.ts` 하나가 URL 경계이자 **"이 측정에서 합법인 뷰"의 정의**입니다. 링크는
> 이 앱이 **자기가 만들지 않은 최초의 뷰**이고, 그 안의 모든 값은 보낸 사람이 보고 있던
> 주행에 대한 주장이라 받는 쪽에 그대로 성립하지 않습니다.
>
> | 받은 링크 | 무엇을 하나 |
> |---|---|
> | 정상 | 같은 주행·같은 순간·같은 파라미터·같은 탭으로 엽니다 |
> | **지운 세션** | 대체 주행을 열되 **seq와 range를 버리고**, 무엇을 왜 바꿨는지 **알립니다** |
> | 주행 끝을 넘는 seq | 마지막 표본으로 당기고 알립니다 |
> | 뒤집힌 range | 끕니다 — 좁은 창이 아니라 **빈 창**입니다 |
> | 이 주행에 값이 없는 KPI | RSRP로 되돌리고 알립니다. 서버만 답할 수 있으므로 분포가 도착한 뒤에 |
>
> 고친 것은 **하나도 조용히 고치지 않습니다.** 조용한 수정이야말로 이 기능이 막으려는
> 실패입니다 — 받는 사람은 **완벽히 자기모순 없는, 보낸 것이 아닌** 화면을 보고, 내일
> 같은 링크를 또 보냅니다.
>
> **검증**: 102/102 · **115/115**(S15 신규 9단계) · api-surface 무결점 · 백엔드 25/25.
> 주입 6건 전부 실패를 증명했습니다.
>
> **주입 R이 제 검사의 구멍을 잡았습니다.** "지운 세션 링크가 그 사실을 알리는가"를
> **알림이 존재하는지**로만 재고 있었는데, seq를 버리는 일은 세션 변경 리셋도 하므로
> **보고 코드를 통째로 지워도 동작은 옳았습니다.** 알림이 **무엇을 적는지**를 재도록
> 바꾸니 실패합니다. §1.5.1과 같은 자리입니다 — 그릇을 재고 내용물을 안 쟀습니다.
>
> ✕ **지도 시점(`v=`)은 URL에 넣지 않았습니다.** 화면에서 가장 공유하고 싶은 값처럼
> 보이지만, **앱도 스스로 쓰는 유일한 값**입니다 — 자동 맞춤이 사용자 이동과 같은
> `moveend`를 냅니다. 빈 페이지를 열기만 해도 아무도 고르지 않은 프레임이 기록되고,
> 다음 로드는 그것을 근거로 첫 맞춤을 건너뜁니다. 조사 단계 blocker 5건 중 **셋이 이
> 파라미터 하나**에서 나왔습니다. 프레이밍은 주행 단위이고 되돌리기는 **F** 한 번이라,
> 보낸 적 없는 프레임으로 열리는 링크보다 작은 손실입니다.
> ✕ **패널 간 시점 연동도 미룹니다.** 지도 두 개를 동시에 띄울 수 있는데, 모듈 싱글턴은
> 이들을 **연동하지 않고 서로 덮어씁니다**. 있다고 주장하느니 없다고 적습니다.


두 종류의 실패가 한 클러스터에 있습니다. **그 순간에 도달할 수 없고**(커서),
**그 순간을 붙잡거나 남에게 건넬 수 없습니다**(상태·URL·줌).

1. ✅ **표본 단위 커서** `s` — ←/→ 한 표본, PgUp/PgDn 열 표본, Home/End, 스텝 버튼,
   속도 1·4·16·64 /s, 역방향. §0의 30표본 건너뛰기가 닫혔습니다
2. ◐ **뷰 상태를 URL에** — 세션·KPI·탭·모드·seq·range·빈 크기·거리 빈·푸트프린트 ✅ /
   **지도 시점** ✕ (위 참조). `replaceState`만 씁니다 — URL은 **주소**이지 히스토리가
   아니라, 재생 중 커서가 초당 수십 번 바뀌면 Back이 "한 표본 전"이 됩니다
3. ✅ **Esc로 모달 닫기 + 최소 키맵** `s` — 사다리 하나, 테이블 하나
4. ◐ **파라미터를 바꿔도 지도 줌 유지** ✅ / **패널 간 시점 연동** ✕ (위 참조)

> ✕ **우클릭 컨텍스트 메뉴는 미룹니다.** 장애추적은 원하고, 베테랑은 무덤덤하고,
> 신입은 *"어차피 발견 못 합니다"* 라며 거부합니다. **신입의 진단이 정확합니다** —
> 셋 다의 진짜 불만은 *범위 좁히기 컨트롤이 화면 맨 아래 상태 바에 있는데 눈은 지도에 있다*는 것입니다.
> **컨트롤 위치를 고치고 제스처는 나중에.**

### ④ 색상 시스템 — **진단용 절반만** `s` — 순위 3 / 6 / 3 — ✅ **완료 (2026-09-01)**

> **결과**: 색을 칠하는 이유가 이제 둘인데 **뜻하는 종류가 반대**입니다. KPI 램프는
> **판정**입니다 — 빨강은 나쁨이고, 같은 색은 어느 주행에서나 같은 평가입니다. 서빙 셀
> 색칠은 **정체성**입니다 — "저것 말고 이것"일 뿐 평가가 없고, 그 주행 안에서만 유효합니다.
> `view/paint.ts` 한 곳이 둘 다 정의합니다.
>
> | | 이전 | 이후 |
> |---|---|---|
> | 한 구간만 보기 | **색상 편집기를 열어 나머지를 회색으로 칠하고, 보고, 되돌리기** — 사적인 질문을 하려고 공유 스케일을 변형 | 범례 행 클릭 |
> | 나머지 구간 | (우회로에서) 회색 | **회색으로 남습니다 — 숨기지 않습니다** |
> | 핸드오버 경계 | 안 보임 | 서빙 셀 색칠에서 **경계가 곧 발견** |
>
> **격리는 숨기기가 아니라 음소거입니다.** 처음 든 생각은 나머지를 지우는 것이었고 그건
> 틀렸습니다 — 나쁜 표본만 남은 경로는 **끊긴 조각들의 흩뿌림**이라 주행 어디쯤인지도,
> 차가 어느 방향이었는지도 알 수 없습니다. 사용자가 손으로 하던 것도 정확히 음소거였습니다.
>
> **팔레트는 이벤트 팔레트를 재사용하지 않았습니다.** 그 아홉 색은 **같은 지도 위에서**
> 이미 아홉 가지 실패를 뜻합니다. 그중 하나가 "PCI 41"도 뜻하게 하면 둘이 함께 그려지는
> 바로 그 자리에서 색이 모호해집니다. 색각 이상에서도 구별되는 Okabe-Ito를 쓰고, **회색은
> 로테이션에서 뺐습니다** — 회색은 "팔레트가 색을 못 준 셀"만 뜻해야 합니다.
> 팔레트보다 셀이 많으면 **색을 돌려쓰지 않고** 회색으로 두고 몇 개인지 적습니다.
> 두 셀이 같은 색이면 **한 셀로 읽히는 틀린 답**이지만, 색이 모자란 건 **없는 답**일 뿐입니다.
>
> **검증**: 107/107 · 115/115 · 주입 5건 전부 증명.
>
> **주입 X가 설계 판단을 확인해 줬습니다** — 음소거 대신 숨기게 만들자 경로 폭이
> 196 → 107 px로 줄었습니다. "어디였는지 알 수 없다"가 픽셀로 나온 셈입니다.
>
> **제 검사의 결함 둘을 잡았습니다.** 경로선을 `stroke-width="6"`으로 세고 있었는데
> 셀 마커와 커서가 width 3이고 음소거된 경로도 width 3이라, 지도 가구를 경로로
> 셌습니다(색 7개 vs 셀 5개). `route-run` 클래스를 붙여 해결했습니다 — ②에서 이웃선에
> 쓴 것과 같은 수법입니다. 또 격리 전후 프레임을 **서로 다른 선택자로** 재고 있었습니다.
>
> **툴바 셀렉트가 위치로 접근되고 있었습니다.** 컨트롤 하나를 추가하자 검사기 네 곳이
> 조용히 엉뚱한 셀렉트를 가리켰습니다. 전부 `aria-label`로 바꿨습니다 — 접근성과
> 검사 안정성이 같은 수정입니다.

순위가 갈린 선을 따라 클러스터가 정확히 둘로 쪼개집니다.

1. ✅ **범례 구간 클릭 → 지도에서 그 구간만** `s` — 세 관점 **전원**이 지목했던 항목
2. ✅ **범주형(값 동일성) 색칠** `l` — 서빙 PCI로 경로선 칠하기. 팔레트는 이벤트
   팔레트를 **재사용하지 않았습니다**(위 참조) — 같은 지도에서 색이 모호해집니다

> ✕ **미룹니다**: 이름 붙은 재사용 가능 색상셋 `m`, 색상셋 카탈로그 뷰 `s`,
> 그리드/Numerical Data를 스케일 색으로 칠하기 `xs`, 그리드 열별 색 바인딩 `m`,
> 셀 마커를 값으로 칠하기 `s`.
>
> 신입이 이 클러스터를 6위로 놓은 이유가 *"핸드오버 경계를 못 본다는 걸 누가 보여주기 전엔 요청할 생각도 못 했을 것"* 입니다.
> **초보가 문제를 아직 못 봐서 낮게 매긴 것은 항목에 대한 반증이 아닙니다** — 그래서 진단용 절반은
> 경험자 두 명 쪽으로 판정합니다. 반대로 **관례 관리용 절반은 신입의 낮은 순위가 유효한 증거입니다**:
> 그 절반은 사용자에게 *이미 관례가 있을 때* 값이 생기니까요.

### ⑤ 공간 조작 — 나머지 `m` — 순위 2 / 3 / 4 — ✅ **완료 (2026-09-01)**

> **결과**: 민원은 **장소**로 옵니다 — *"차고지 지나 순환도로에서 끊깁니다"*. 그런데
> 장소를 물을 방법이 없어서, **시간으로 손수 번역**해야 했습니다: 커서를 한쪽 끝에 놓고
> Mark from, 반대쪽 끝으로 끌고 가서 Mark to. 그 도로를 세 번 지났으면 **세 번 반복하고,
> 합칠 방법은 아예 없었습니다.** 도로는 하나의 질문이고, 세 번의 통과는 차가 어떻게
> 달렸는지의 부산물일 뿐입니다.
>
> | | 이전 | 이후 |
> |---|---|---|
> | 장소를 묻기 | 통과마다 수동 구간 표시, 합산 불가 | 지도에 도형을 그리면 **통과 전부의 통계** |
> | 통과 구분 | — | 집계 **옆에** 통과 목록 — 클릭하면 그 통과로 |
> | 두 빌드 비교 | *"B가 2.1 dB 나쁨"* | **타일별 차분 지도** |
>
> **집계만 내면 안 됩니다.** *"이 거리가 나쁘다"* 와 *"세 번 중 한 번이 나빴다"* 는 **같은
> 평균**을 내지만 원인도 대책도 다릅니다. 통과 목록이 그 둘을 가릅니다.
>
> **차분은 두 주행을 하나의 격자**에 담습니다. 각자의 중심으로 비닝하면 타일 크기가 미묘히
> 달라져 격자가 지도 전체에서 어긋나고, 뺄셈이 **서로 다른 땅** 사이에서 일어납니다 —
> 권위 있어 보이는데 무관한 숫자들의 산수입니다.
>
> **한쪽만 지나간 타일의 차이는 `null`이지 0이 아닙니다.** *"둘 다 재고 차이가 없었다"* 와
> *"한쪽만 가 봤다"* 는 정반대의 발견인데 0은 앞의 것을 주장합니다. 지도에서도 **속을 채우지
> 않고 점선 테두리**로 그립니다.
>
> **PostGIS를 넣지 않았습니다.** 술어 하나를 위해 모든 설치에 배포 요구사항을 얹는 대가로
> 얻는 것이 **11줄짜리 짝홀 광선 판정**입니다. 스키마에 다른 용도도 없습니다.
>
> **검증**: 107/107 · **126/126**(S16 신규 11단계) · api-surface 무결점 · 백엔드 25/25.
> 주입 5건 전부 증명.
>
> **주입 AE가 제 코드의 중복을 잡았습니다.** `colorFor`와 `labelFor`가 각자 방향을
> 계산하고 있어서, 한쪽만 뒤집자 **빨갛게 칠해 놓고 "better"라고 적는** 타일이 나왔고
> 두 함수 다 개별적으로는 그럴듯해서 아무 검사도 눈치채지 못했습니다. 판정을 한 번만
> 내리도록 합쳤습니다.
>
> **제 검사 셋이 무의미했습니다.** ① "두 꼭짓점은 영역이 아니다"를 **버튼 비활성화**로만
> 재고 있어서 서버 규칙을 바꿔도 통과했습니다. ② "모든 통과를 담는다"를 헤더 총계와
> 비교했는데 **그 총계가 통과 목록의 합**이라 잘라내도 둘 다 같이 줄었습니다 — 통계 쿼리의
> 값 개수(독립적인 두 번째 쿼리)와 비교하도록 고쳤습니다. ③ "한쪽만 지나간 타일" 검사가
> **범례의 고정 문구**를 읽고 있었습니다.
>
> **주입 하네스도 고쳤습니다.** 외부 타임아웃에 프로세스가 죽으면 `finally`가 못 돌아
> **주입된 결함이 작업 트리에 남습니다.** 실제로 한 번 남았습니다. 스냅샷을 파일로도
> 써 두고 다음 실행이 되돌리게 했습니다.

1. ✅ **지도에 임의 폴리곤 → 그 영역 통계** `m` — 통과 목록이 집계 옆에 함께 나옵니다
2. ✅ **두 세션의 공간 차분 지도** `m` — 한 거리인지 전체인지가 타일로 보입니다

### ⑥ 워크벤치 — 셋 중 **한 조각만** — 순위 1 / 5 / 6 (가장 크게 갈린 곳) — ✅ **완료 (2026-09-01)**

> **결과**: 캔버스가 **표본에 붙은 것을 아무것도 읽지 못했습니다** — 속도·위치·서빙 셀은
> `sample_kpi`가 아니라 `sample`에 있습니다. 세 관점이 각자 든 예시 KPI **둘 다** 그것을
> 필요로 했습니다(*"움직이는 동안의 RSRP 마진"*, *"PCI 41이 서빙일 때의 BLER"*).
>
> 그리고 노드가 무엇을 내는지 보려면 **버려질 KPI를 발행**해야 했습니다 — 전 세션에 행을
> 쓰고 모두가 보는 카탈로그에 항목을 추가하고, 다른 화면에서 확인하고, 돌아와 삭제.
> **추측할 때마다** 이걸 했고, 그걸 하는 사람은 **가장 확신이 없는 사람**입니다.
>
> | | 이전 | 이후 |
> |---|---|---|
> | 표본 필드 | 캔버스에서 **도달 불가** | `SOURCE_SAMPLE` — 위경도·속도·서빙 PCI |
> | 이벤트 | 〃 | `SOURCE_EVENT` — ②의 레지스트리에서 타입명을 읽습니다 |
> | 노드 확인 | 발행 → 확인 → 삭제 | **아무것도 쓰지 않는** 미리보기 |
>
> **이벤트는 값이 1이고 나머지는 NULL이지 0이 아닙니다.** 0으로 두면 다른 모든 표본이
> *"이벤트 없음"이라는 실측*이 되는데, 로그는 그런 주장을 한 적이 없습니다.
>
> **미리보기의 행 수는 노드 전체를 셉니다.** *"3행"* 과 *"41,000행 중 처음 3행"* 은
> *"내 조인이 의도대로 됐나"* 에 대한 **다른 답**이고, 페이지만으로는 구별할 수 없습니다.
>
> **표본 소스는 KPI 의존성을 주장하지 않습니다.** 그 목록이 재계산 순서를 정하므로,
> 읽지도 않는 KPI를 적으면 관계없는 편집이 재계산을 유발합니다.
>
> **검증**: 107/107 · **135/135**(S17 신규 9단계) · api-surface 무결점 ·
> 백엔드 **30/30**(그래프 컴파일러 단위 테스트 5개 추가). 주입 5건 전부 증명.
>
> **레코드에 필드를 두 개 더한 것만으로 단위 테스트 8곳이 깨졌습니다.** `-DskipTests`로
> 컴파일하다 컨테이너 빌드에서야 드러났습니다 — 테스트 컴파일을 건너뛴 검증은 검증이
> 아닙니다.
>
> **주입 AI·AJ가 제 시나리오의 결함을 잡았습니다.** S17이 **자기 뒤처리를 하지 않아서**,
> 일부러 발행하게 만든 주입이 KPI 두 개를 DB에 남겼고 다음 실행의 기준값을 망가뜨렸습니다.
> S13은 이미 같은 교훈을 배웠는데 S17이 물려받지 못했습니다. 이제 **측정 전에도** 쓸어냅니다.

**"만드는 사람 대 보는 사람"의 차이**입니다. 평균내면 안 됩니다.

1. ✅ **`SOURCE_SAMPLE` 노드** `s` — 허용 목록 방식이라 문서가 컬럼명을 정할 수 없습니다
2. ✅ **`SOURCE_EVENT` 노드** `m` — ②의 레지스트리 위에
3. ✅ **중간 노드 결과 미리보기** `m` — 행 수 + 표본 행. 발행 없이, 카탈로그 등재 없이

> **③의 미리보기를 신입 개인의 불편으로 보면 순위를 잘못 매깁니다.**
> 지금 우회로는 *KPI 이름을 지어내 발행 → 전 세션에 행을 쓰고 카탈로그에 모두가 보는 항목을 추가 →
> 다른 화면에서 확인 → 돌아와 삭제*이고, 이걸 **추측할 때마다** 합니다.
> 즉 **가장 경험 없는 사용자가 공유 상태를 매 시도마다 변형**합니다. 개인 불편이 아니라 **다중 사용자 무결성 문제**입니다.

> ✕ **미룹니다**: 재사용 부분 그래프(Components)와 이름 붙은 상수 `l` —
> **세 관점 전원의 "안 해도 됨" 목록**에 있고 클러스터에서 가장 큰 항목입니다.
> ✕ **생성 SQL 패널** `xs` — 셋 중 둘이 *"읽지 않겠다"* 고 명시했습니다.
> 그 밑의 진짜 요구(*"Combine이 내가 의도한 척추로 조인했나"*)는 **미리보기가 더 잘 답합니다.**
> SQL 패널 대신 미리보기에 **행 수와 조인된 표본 행**을 넣습니다.

### ⑦ 통계·분포 화면 — 순위 5 / 5 / 6

**일관되게 낮지만, 낮은 이유가 셋 다 다릅니다.** 아무도 막히지 않고, 대신
**틀렸다는 걸 알 방법 없이 틀린 숫자를 제출하게** 됩니다. 늦은 자리가 맞지만 빠질 자리는 아닙니다.

1. **기준과 도메인을 표기** `xs` — 하드코딩된 `[Sample]` 범례 머리글을 풀고,
   평균에 **"dB 도메인"** 을 명시. 통계 패널·리포트·내보내기 전부
2. **거리 기준 집계를 선택 가능한 기준으로** `l` — 표본 기준은 기본값 유지.
   빌드 비교의 **판정을 바꾸는** 정차 편향이 여기서 사라집니다. **①의 GPS 방어 이후에**
3. **선형 도메인 평균을 별도 이름의 옵션으로** `m` — **절대 기본값이 아니고, 절대 조용히 바꾸지 않음**

> dB 평균은 **삼자 대립**입니다. 베테랑은 반대(*"내 숫자가 남들과 어긋납니다"*),
> 신입은 조용히 고쳐지길 원하고, 장애추적은 티켓용으로 필요합니다.
> **계산 요청이 아니라 표기 실패입니다** — 라벨을 먼저 붙이면 세 입장이 동시에 만족됩니다.

> ✕ **미룹니다**: 구간 경계 포함 방향 `m` (셋 다 낮게 봤고 둘이 같은 우회로를 제시 — *"-100.1을 치고 넘어간다"*),
> 밀도(PDF)와 사용자 지정 구간 `m`.

### ⑧ 세션·워크스페이스 관리 — 순위 **7 / 7 / 7** (만장일치 최하위)

> ⚠ **이 만장일치는 표본 편향입니다.** 세 관점 모두 주행을 **받는** 사람이고,
> **임포트하는 사람이 없습니다.** *"테스터의 고통이지 내 것이 아니다"* 라는 말이 세 번 나옵니다.
> 미루되, **세 명이 반대해서가 아니라 그 사람에게 아직 안 물어봤기 때문**이라고 적어 둡니다.

- 임포트 진행률(행 수)과 **작동하는 취소** `m`
- 폴더 통째로 로드 + 장비·사업자·기술 값 **임포트 간 유지** `s`
- 세션 목록 검색·필터(날짜·장비·사업자·지역) `s`

> ✕ Projects(저장된 세션 집합) `m` — 캠페인 관리. 베테랑은 8년간 머릿속에 담아 왔습니다.

---

## 2. 싼 것부터 — 큰 덩어리 뒤에 줄 세우지 말 것

전부 `xs`~`s`인데 효과가 큽니다. **클러스터 순서와 무관하게** 먼저 할 수 있습니다.

> **남은 셋(4·8·10)은 각각 ④·⑥·⑦의 첫 항목입니다.** 즉 남은 세 클러스터의 가장 값싼
> 조각이 이미 여기 모여 있습니다 — 클러스터를 통째로 순서대로 하는 대신 이 셋을 먼저
> 묶어 내는 선택지가 있습니다.

| # | 항목 | 크기 | 왜 |
|---|---|---|---|
| ~~1~~ | ~~경로선의 결측 구간을 **끊어서** 그리기 + GPS 튐 방어~~ | `s` | ✅ **완료.** 17.82 km → 4.40 km |
| ~~2~~ | ~~지도에 **이벤트 타입별 심볼**~~ | `s` | ✅ **완료(②).** 6클릭+암기 → 한 눈 |
| ~~3~~ | ~~차트에 **이벤트 마커**~~ | `s` | ✅ **완료(②).** *"먼저였나 나중이었나"* 를 답합니다 |
| ~~4~~ | ~~**범례 구간 클릭** → 지도에서 격리~~ | `s` | ✅ **완료(④).** 음소거이지 숨기기가 아닙니다 |
| ~~5~~ | ~~**표본 단위 커서**(화살표·스텝·속도)~~ | `s` | ✅ **완료(③a).** 재생이 표본을 건너뛰지 않습니다 |
| ~~6~~ | ~~**뷰 상태를 URL에**~~ | `s` | ✅ **완료(③b).** 지도 시점은 제외 — 이유는 ③에 |
| ~~7~~ | ~~**Esc로 모달 닫기** + 최소 키맵~~ | `s` | ✅ **완료(③a).** 사다리 하나, 테이블 하나 |
| ~~8~~ | ~~**`SOURCE_SAMPLE` 노드**~~ | `s` | ✅ **완료(⑥).** 두 명이 최하위로 매긴 클러스터의 만장일치 항목 |
| ~~9~~ | ~~임포트 **중복 이름 감지 + 설명 필드**~~ | `s` | ✅ **완료.** 중복은 거부하고 어느 세션이 이름을 쥐고 있는지 알려 줍니다 |
| 10 | `[Sample]` **하드코딩 해제** + 도메인 표기 | `xs` | 계산을 안 바꾸고 dB 삼자 대립을 해소 |

---

## 3. 세 관점이 합의한 것

- **세션 관리가 꼴찌** (7/7/7) — 다만 위 ⑧의 표본 편향 단서와 함께 읽을 것
- **경로선의 거짓** — 유일하게 세 명이 거의 같은 문장으로 지목
- **이벤트 클러스터에서 아무것도 미루지 않음** — 그리고 각자 **다른 항목**을 최우선으로
- **커서가 필요한 표본에 닿지 않음** — 셋이 독립적으로
- **새로고침 손실과 공유 불가** — 셋 다 *"증거를 넘기는 방식"* 이라는 같은 2차 영향을 지목
- **범례 구간 클릭** — 색상에 무관심한 신입까지 포함해 전원
- **그래프가 위경도·속도·PCI를 못 읽음** — 전원, 그리고 둘이 같은 예시 두 개
- **만장일치 보류 셋**: Components/상수(`l`), 색상셋 카탈로그, 지도 거리 측정

## 4. 갈린 곳과 그 의미

| 클러스터 | 베테랑 | 신입 | 장애추적 | 판정 |
|---|---|---|---|---|
| events-visual | 1 | 4 | 1 | 통째로, 앞쪽 |
| 기본 조작 표면 | 4 | 2 | 2 | 앞쪽 |
| 공간 조작 | 2 | 3 | 4 | 중간 |
| 색상 시스템 | 3 | **6** | 3 | **쪼갬** — 진단용만 앞으로 |
| workbench-ux | **1** | **5** | **6** | **쪼갬** — 소스 노드만 앞으로 |
| statistics-ui | 5 | 5 | 6 | 뒤쪽, 표기부터 |
| 세션 관리 | 7 | 7 | 7 | 미룸 (편향 단서 첨부) |

**가장 크게 갈린 workbench-ux(1/5/6)는 중요도 차이가 아니라 "만드는 사람 대 보는 사람"입니다.**
셋 중 신입만 **산출물을 만들라는 요구**를 받는 사람이고, 나머지 둘은 매일 지도를 보되
KPI는 2주에 한 번 만들거나 아예 안 만듭니다. 평균내면 둘 다 틀립니다.

---

## 5. 지금 하지 않을 것

| 항목 | 왜 |
|---|---|
| 재사용 부분 그래프(Components) · 이름 붙은 상수 `l` | 세 관점 전원의 보류 목록. 이걸 원하는 사람은 **KPI 라이브러리 관리자**인데 셋 중 아무도 그 역할이 아닙니다 |
| 생성 SQL 패널 `xs` | 싸지만 둘이 읽기를 거부. 밑의 진짜 요구는 **미리보기가 더 잘 답합니다** |
| 색상셋 카탈로그 뷰 `s` | 전원 보류 |
| 지도 거리 측정 `s` | 전원 보류 — 베테랑은 플래닝 툴에 더 정확한 좌표가 있습니다 |
| 우클릭 메뉴 `m` | 진짜 불만은 **컨트롤 위치**. 위치를 고치고 제스처는 나중에 |
| 구간 경계 포함 방향 `m` | 전원 낮게, 둘이 같은 우회로 제시 |
| 밀도(PDF)·사용자 지정 구간 `m` | 분포 형태는 **조사(survey) 질문**이지 장애 추적 질문이 아닙니다 |
| Projects(세션 집합) `m` | 캠페인 관리. 검색·필터가 먼저 |
| 이름 붙은 재사용 색상셋 `m` | 사용자에게 **이미 관례가 있을 때** 값이 생깁니다. 아직 이릅니다 |

---

## 6. 이 문서 밖에 있는 UI 항목

브리프의 "남은 일" 표에서 나왔지만 위 7개 클러스터에 안 들어간 것들입니다.
크기가 작고 독립적이라 위 순서에 끼워 넣기 쉽습니다.

| 항목 | 크기 | 어디에 붙일까 |
|---|---|---|
| **드릴다운 다중 탭** (조각 색 유지) | 중 | ② 뒤 — 여러 원인을 동시에 열어 두는 기능 |
| 파라미터 **★ 즐겨찾기** | 매우 작음 | ③과 함께 |
| 사용자 KPI의 **출처 표시**(`User` 분류) | 작음 | ⑥의 미리보기와 함께 — 발행된 KPI의 출처가 보여야 |
| **전역 필터** | 중 | ⑤ 폴리곤 뒤 — 폴리곤이 전역 필터의 첫 입력 |
| **color grid · surface** 차트 | 중 | ⑦ 뒤 — 두 파라미터 상관을 보는 유일한 뷰 |
| 워크북 **레이아웃 프리셋** · 기성 워크북 **복사** | 작음 | 독립 |
| 리포트 **구성 선택** · 워크북 **PDF/이미지** 내보내기 | 중/작음 | 독립 |
| 푸트프린트 **3강 기준** · 거리 빈 **40λ** · **Cell locator** | 작음~중 | ⑤와 함께 |
| 식 언어에 **함수 도입**(허용 목록) | 작음 | ⑥과 함께 — 파서가 이미 허용 목록 설계 |

---

> **근거 문서**: 클러스터별 코드 대조 결과와 세 관점의 원문 순위는 워크플로 산출물에 있고,
> 각 항목의 출처(매뉴얼 페이지)는 [`docs/briefs/`](briefs/)와
> [`docs/reference/nemo-analyze-10.2/`](reference/nemo-analyze-10.2/)에 있습니다.
