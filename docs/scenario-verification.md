# 시나리오 단위 E2E 검증

실행 일자: 2026-08-30 · 도구: `scripts/verify-scenarios.mjs` (Playwright, 실제 브라우저)

`verify-ui.mjs`(30개)가 개별 동작을 확인한다면, 이 검증은 **사용자 여정 전체**를 순서대로
걷습니다. 각 시나리오는 실제 사용자가 그 상황에서 밟는 단계의 연속이며, 단계 간 상태(커서
위치, 선택된 세션, 필터)가 이어집니다. 중간 단계가 막히면 그 시나리오는 실패입니다 —
여정 중간에 막힌 사용자에게 나머지 화면이 렌더링되는지는 중요하지 않기 때문입니다.

실행:

```
$ node scripts/verify-scenarios.mjs
49/49 steps passed        # 2026-08-30 기준
```

## 시나리오 카탈로그와 결과

| # | 시나리오 | 사용자 | 여정 | 결과 |
|---|---|---|---|---|
| S1 | 주행 후 필드 분석 | RF 엔지니어 | 세션 열기 → 색상 경로 → 범례 합계 검산 → 열화 구간 → 클릭 점프 → 그리드 확인 → L3 드릴다운 → 이벤트 점프 → 셀 테이블 → 재생 → CSV 내보내기 | 12/12 |
| S2 | 빌드 A/B 비교 | 검증 팀 | 기본 비교(1.4.2 vs 1.5.0) → KPI별 통계·판정 → CDF 오버레이 → 상대 세션 교체 시 재계산 | 5/5 |
| S3 | 랩 캠페인 (가상 채널 + 가상 UE + 실제 DU) | 랩 운영자 | 캠페인·런 목록 → 에뮬레이트/실물 구성 확인 → 필드 리플레이 채널 → 판정 실행 → **프론트홀 게이트 판정(무선 PASS + 프론트홀 FAIL)** → 실패 런에서 세션으로 드릴스루 | 7/7 |
| S4 | 프론트홀 결함 분류 | 트러블슈터 | 증상(처리량 하락) → CUS RX late CRITICAL 구간 → 구간 내부: RF·BLER 정상 + 처리량 저하 + 프론트홀 CRITICAL → Fronthaul 워크북 → 이벤트·M-plane 알람 | 7/7 |
| S5 | 커버리지 최적화 | 망 설계자 | 영역 비닝 → 원인별 커버리지 문제 → 위치 점프 → GeoJSON 내보내기 (+ 필드 세션의 프론트홀 빈 상태 설명) | 5/5 |
| S6 | 데이터 수명주기 | 데이터 관리자 | CSV 내보내기 → 브라우저 업로드로 재임포트(1200/1200, 누락 0) → 이력 기록 → 임포트 세션 분석 → 툴바에서 삭제 | 5/5 |
| S7 | 통계 리포팅 | 리포트 작성자 | 요약 통계 → 분위수 순서 검산 → CDF + p05/p50/p95 마크 → KPI 전환 → **범위 필터(칩 표시, 통계·범례 축소, 해제 시 복원)** | 6/6 |
| S8 | 응답성 예산 | (비기능) | 분석 엔드포인트 8종 각 1.5초 이내 (실측 최악 48 ms) — 시드 초과 규모는 `load-test.sh` 담당 | 1/1 |

교차 확인: 전 여정에서 앱 코드 콘솔 오류 0건 (베이스맵 타일 실패는 샌드박스 네트워크 제약).

## 검증이 실제로 잡아낸 것

이 검증을 만드는 과정 자체가 결함을 찾았습니다. 기록해 둡니다.

1. **결함 창과 페이드 중첩 (데이터 결함)** — 프론트홀 주입 세션의 결함 창(seq 380–520)이
   리플레이 채널의 RSRP 딥페이드(479–501 CRITICAL)와 겹쳐 있었습니다. "무선은 정상인데
   프론트홀만 이상"이라는 시나리오의 핵심 서사가 시드 데이터에서 성립하지 않았던 것입니다.
   → RF가 깨끗한 seq 885–985로 이동. 동시에 물리를 정직하게: 수신 창을 놓친 데이터는 DU가
   폐기하므로 **처리량·PRB 사용률은 결함 창 안에서 하락**하고 RSRP/SINR/BLER은 무변.
   분류 시그니처가 "전부 정상"이 아니라 "처리량 하락 + RF 무결 → 전송로"로 바뀌어
   실제 트러블슈팅 흐름과 일치합니다.
2. **커서-디시메이션 단절 (코드 결함)** — 차트 판독값과 지도 커서가 `seq === cursorSeq`
   정확 일치로 표본을 찾아, 서버가 데시메이션한(=대용량) 세션에서 'no data'와 마커 멈춤이
   발생. 대용량 최적화가 만든 정확성 구멍이었고, 커서 이하 최근접 표본 탐색으로 수정.
3. **비교의 거짓 SAME (코드 결함)** — 한쪽 세션에 없는 KPI의 비교 판정이 SAME으로 표기.
   측정하지 않은 것과 같은 것은 다르므로 NO DATA 판정을 신설.
4. **차트 없는 KPI 12종 (도달성 결함)** — 사전 로드 6종 외 KPI 선택 시 차트가 조용히
   사라짐. 제품 차별점인 프론트홀 KPI 전부가 여기 포함되어 있었음. 온디맨드 시리즈로 수정.

앞의 세 검증 스크립트(`verify-ui.mjs` 30 · `verify-scenarios.mjs` 49 · `api-surface.mjs`)는
독립적으로 유지합니다: 첫째는 개별 동작 회귀, 둘째는 여정 회귀, 셋째는 "로직은 있는데 뷰가
없는" 종류의 격차를 잡습니다.

## 이번 라운드 반영 항목

검토(3개 독립 리뷰) 결과와 반영·보류 내역은 `docs/gap-analysis.md` §5에 정리했습니다.
요약: Fronthaul 워크북, 프론트홀 게이트 판정 런, 범위 필터(+칩), 세션 삭제, 랩→분석
드릴스루, Compare CDF 오버레이, 재생, 셀 테이블, 임포트 헤더 동의어, 커서/비교 결함 수정,
kpi_rollup 제거, FR 과대표기 정정.
